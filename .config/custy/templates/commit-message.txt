fix(workflow): release Custy v2.1.1

Correct version reporting and make the daily development profile honor its
documented commit-and-push boundary.

### Fixed

- Replace setuptools-scm's post-release scheme with development-version
  inference so an exact stable tag reports `2.1.1` instead of an artificial
  `.post0` suffix.
- Keep the source fallback version synchronized at `2.1.1`.
- Remove version preparation, workflow initialization, release-artifact
  generation, changelog generation, version application, and tagging from
  `custy run dev`.
- Open only the commit-message file during the Dev profile.
- Push branch `HEAD` without attempting to push a resolved-but-uncreated tag.
- Preserve the existing release and full-profile tag behavior through explicit
  per-step intent.

### Tests and documentation

- Add profile, step, editor, push-orchestration, and remote-push regression
  coverage for commit-only development execution.
- Update the English and Indonesian documentation baseline and Dev workflow
  guidance for v2.1.1.

Tag: v2.1.1
Previous version: v2.1.0
Changelog: generated separately

Tags: #fix #workflow #versioning #cli #docker #documentation
