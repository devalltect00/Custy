I need you to understand current Custy project I have. Please understand in detail. I will send the related files one by one. then I need to update some files. Mostly it is about UI, UX, testing, and documentations.
So priority or the order or the steps to update overall are:

1. UI and UX (Done)
2. Testing (Done)
3. Documentations
4. Meta data

Details:
For UI and UX, I will send all my files related to help message for CLI in `app/cli/commands`. mostly the file name is command.py. So I just need to update the UI and the UX. If the UI / UX is already good then it is okay. The UI it using CLI with rich, console, and other libraries. Just update the help message. Adjust to current command or files how it works or available commands, etc.

Then testing part, where we make a tests for those files. Idk it is good or not to have tests with 100% coverage. but let's see.

For documentation. I will follow like this structure

```
├── docs
│   ├── architecture (This is related to detailed docs or documentation about architecture projects)
│   │   ├── examples.md  (This document contains practical examples. Examples range from local testing to production publishing workflows. Or ther examples)
│   │   ├── faq.md (This document answers common questions about the command)
│   │   ├── overview.md (Overview related to the command)
│   │   ├── requirements.md (This is related to requirements need to run the apps or tools. but specifically the command or this command. For example some requirements.md they need the user to have git or docker installed. Some of them are not required anything and can just use apps or tools and run the command)
│   │   └── workflow.md (This document describes how the Dockerize workflow operates internally.)
│   ├── commands (This is documentation or docs related to commands available for the projects or tools)
│   │   ├── design-patterns.md  (This is related to design pattern used)
│   │   ├── diagrams.md (This is the diagrams related to this architecture)
│   │   └── workflow.md (This is related to architecture workflow)
│   ├── developer-guide (This is docs as guides and the are developers or among developers)
│   │   ├── blackbox (This is just reads or documentation how the project related to blackbox)
│   │   ├── tooling (This is just reads or documentation how the project related to some tools)
│   │   ├── developer-guide.md (This is docs as guides and the are developers or among developers. Overall docs or documentations)
│   │   ├── docker-workflow.md (This is related to how developers use docker or related to docker usage from the certain project)
│   │   └── getting-started.md (This is related to how developers getting started related to the certain project)
│   ├── diagrams (This is a collection of diagrams related to project. This can contain md files or documentation like architecture diagrams, workflow diagrams, activity diagrams, menu design diagrams, flowchart diagrams, algorithm diagrams, models diagrams, use case diagrams)
│   │   ├── generated
│   ├── project (This is related to project overall)
│   ├── reads (This is related other things just for reads or readings)
│   ├── testing (This is related to the documentation related to testing or test or testing or do some tests or do some testing to the project)
│   │   └── testing-guide.md
│   ├── user-guide (This is docs as guides and the target are users or any users)
│   │   ├── commands.md (Available commands the users can use)
│   │   ├── installation-methods.md (Installation method the users can use)
│   │   ├── lifecycle.md (Reads for users related to lifecycle usage)
│   │   ├── overview.md (Overview related to the project)
│   │   └── quickstart.md (Quick start guide for user related to usage)
│   ├── badges.md (Bagdes related to project)
│   ├── configuration.md (Configuration docs related to project)
│   ├── how-to-use.md (how-to-use docs related to project)
│   ├── index.md (Entrypoint docs related to project or just an index.md file)
│   ├── infrastructure.md (infrastructure docs related to project)
│   ├── installation.md (Installation related docs related to project)
│   ├── project_structure.md or PROJECT_STRUCTURE.md (project structure related to project. HINT: actually these files created or generated by doc-gen project by running doc-gen structure generate command when the tools or apps already installed or run by using docker image)
│   ├── TODO_tracking_history.md (This is just TODO files, but ignoreable)(For this no need to generate this one)
│   └── usage.md (This documentations or docs is the technical reference companion. Adcvanced references)
│   TODO.md
└── README.md (This README.md related to README.md from the root directory for the certain projects)
```

For Metadata to update there are

- pyproject.toml
- CONTRIBUTING.md
- SECURITY.md
- AGENTS.md
- .blackbox/skills/\*\*/SKILL.md

Are you ready?

=============

Wait before go to the `app/cli`. I will sent all the backend logic workflow before go to the frontend to understand well.

So overall the flow is like

User -> CLI -> resolve the user input -> go to the core parts or call same class or functions or methods there. I can't explain everything because it is too complex and too many. instead I will send the files or related files one by on.

So we start from core or backend to the cli or frontend

Just as note. if made updates use better languages.

Got it?

=============

Let's start with Banner related

For banner it use Pyfiglet or figlet
I sent you:

- `app\services\banner_service.py`

Related to CLI theme
I sent you `app\theme\theme.py`

Please review it. Next is Related to configuration loader

=============

Related to configuration loader
I sent you:

- `app\config\config_loader.py`, is thhe loader script
- `config.toml`, is the file that being loaded

Related inside app/core/

Please review it. Next is Related to Dryrun parameter options and logging or logger

=============

Just remember all of them, including the plan related, UI, Tesing, Documentations, or meta data that will be updated. Remember the list of updated or new files.

Related to Dryrun parameter options and logging or logger
I sent you:

- dry_run_support.py
- dry_run.py
- logging.py

Please review it. Next is Related to UI tools inside `app/ui/`

=============

Related to UI tools or script for CLI
I sent you these files related to that

Also I give you my current CLI looks like and available overall command. (NOTE: just surface command, not all commands and not including the sub commands)

Currently if I ran Custy

`````

(venv) D:\project\backups\git_commit\custy-2>custy

  .,-:::::  ...    ::: .::::::.::::::::::::.-:.     ::-.
,;;;'````'  ;;     ;;;;;;`    `;;;;;;;;'''' ';;.   ;;;;'
[[[        [['     [[['[==/[[[[,    [[        '[[,[[['
$$$        $$      $$$  '''    $    $$          c$$"
`88bo,__,o,88    .d888 88b    dP    88,       ,8P"`
  "YUMMMMMP""YmmMMMM""  "YMmMY"     MMM      mM"

                  v1.10.14.post1


 Usage: custy [OPTIONS] COMMAND [ARGS]...

 🚀 Custy — Git Automation CLI

 Automate your Git workflow with structured commits, versioning,
 changelog generation, and release pipelines.

 Designed for speed, consistency, and automation

 ────────────────────────────────────────

 📦 Core Capabilities:

   • Commit with structured messages
   • Tag versions using SemVer / PEP 440
   • Generate changelogs automatically
   • Automate full release pipelines

 ────────────────────────────────────────

 ⚡ Common Commands:

   custy init
       Initialize project (config, templates, examples)

   custy commit
       Create a structured commit

   custy tag
       Create or bump version tag

   custy push
       Push commits and tags to remote(s)

   custy changelog
       Generate or manage changelog

 ────────────────────────────────────────

 🔁 Pipeline Usage:

   Run multiple steps in one command:

     custy run commit tag push
     custy run release

   Presets like "release" expand into full workflows

 ────────────────────────────────────────

 🧠 Getting Started:

   1. custy init
   2. custy run commit tag

   Optional:
     custy run release

 ────────────────────────────────────────

 🧰 Additional Tools:

   custy validate     Check project readiness
   custy backup       Backup message templates
   custy cleanup      Remove old backups & branches
   custy version      Sync version across files

 ────────────────────────────────────────

 ❓ Need Help?

   Use --help on any command:

     custy run --help
     custy tag --help

 ────────────────────────────────────────

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ --no-banner                     Disable banner                                                                                   │
│                                 Default: False                                                                                   │
│ --help                -h        Show help                                                                                        │
│ --version             -v        Get app version                                                                                  │
│ --install-completion            Install completion for the current shell.                                                        │
│ --show-completion               Show completion for the current shell, to copy it or customize the installation.                 │
╰──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Global • Execution ─────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ --dry-run  -dr  --no-dry-run  -Dr    Dry run mode                                                                                │
│                                      Simulate execution without making changes.                                                  │
│                                      ✔ No Execution.                                                                             │
│                                      Useful for testing workflow safely.                                                         │
│                                      HINT:: Useful for debugging                                                                 │
╰──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Global • Debug ─────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ --debug      -dbg  --no-debug  -Dbg                                       Debugging mode                                         │
│                                                                           Show/Hide debug message.                               │
│ --log-level  -ll                     [critical|error|warning|info|debug]  Logging level                                          │
│                                                                           Control verbosity of logs                              │
│                                                                           • critical → only critical errors                      │
│                                                                           • error    → errors only                               │
│                                                                           • warning  → warnings + errors                         │
│                                                                           • info     → general info (default                     │
│                                                                           • debug    → detailed debugging                        │
│                                                                           Default: info                                          │
│                                                                           HINT: better using .custy.toml configuration.          │
│                                                                           Variable: 'tool.custy.logging.level'                   │
╰──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ init       Initialize your project                                                                                               │
│ validate   Validate your project setup                                                                                           │
│ commit     Create a commit                                                                                                       │
│ tag        Create a version tag                                                                                                  │
│ push       Push changes to remote                                                                                                │
│ run        Run a pipeline of steps                                                                                               │
│ changelog  Changelog management                                                                                                  │
│ version    Version management                                                                                                    │
│ backup     Backup message files                                                                                                  │
│ cleanup    Cleanup project resources                                                                                             │
╰──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ (still in development) ─────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ workflow   Handle Git workflow transitions (develop → release → main).                                                           │
╰──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯

`````

As you can see what CLI looks.

The next is related to core logic for those specific commands. NOTES that Please understand the custy

=============

Related to command like `custy commit`, `custy tag`, `custy commit tag`, `custy push`, `custy commit push`, `custy commit tag`, `custy dev`, `custy release`, `custy full`. etc
I sent these files. I can't sent all the files because it is too many. But I explain a bit.

Start by command like `custy version update` and `custy changelog generate`

Related to update files. for example when run `custy version update`
I sent you:

- `app\core\files\update_files.py`

Related to generate changelog like CHANGELOG.md file. for example when run `custy changelog generate`

- The `app\core\git_ops\versioning\builder\release_builder.py` basically script to generate changelog. For example CHANGELOG.md. The related files like `app\core\git_ops\versioning\models\release_info.py` and `app\core\git_ops\versioning\models\version_type.py`. And also `changelog.j2` I send is the templates used
- The `app\core\git_ops\versioning\version_bridge.py`, basically a bridge among different tag format startegy
- `app\core\git_ops\commit\validator.py` Is where it validate the commit message. the commit message should follow specific formats defined.

=============

Next is

Related to git Operations, I sent you these

- `app\core\git_ops\git\executor.py`
- `app\core\git_ops\git\factory.py`
- `app\core\git_ops\git\protocol.py`
- `app\core\git_ops\git\result.py`
- `app\core\git_ops\git\service.py`

As you can see how it being architect and designed. As you see the layers, there are many layers.

Please review it. Next is Related to tag strategy

=============

Related to the tag strategy, I send you these files. I think it use the strategy design pattern, right?

Please review it. Next is Related to git operations helper

=============

Related to git operations helper, I send you these files:

- `app\core\git_ops\helper\commitizen.py`
- `app\core\git_ops\helper\pep440_helper.py`
- `app\core\git_ops\helper\project_detector.py`
- `app\core\git_ops\helper\semver_helper.py`
- `app\core\git_ops\helper\version_helper_base.py`
- `app\core\git_ops\helper\version_utils.py`

Please review it. Next is Related to command like `custy backup commit` and `custy backup tag` and `custy backup all` and `custy cleanup backups` and `custy cleanup branches` and and `custy cleanup all`

=============

Related to backup command:
I sent you:

- `app\core\backup\backup_manager.py`

Related to cleanup command:
I sent you:

- `app\core\cleanup\branch_cleaner.py`
- `app\core\cleanup\handle_cleanup_backups.py`
- `app\core\cleanup\handle_cleanup_branches.py`

Also Additionally to command `custy workflow`. Note that this is still in development. or the beta version. Actually the script is done for now. but still need more review before become the relase. So remember make sure to add this warning on CLI help message for `custy workflow` on the `--help` part

Related to workflow command (Still in development. or the beta version):
I sent you:

- `app\core\branch_workflow\branch_workflow_manager.py`

Please review it. Next is Related to command like `custy run`

=============

Related to run command or pipeline architecture part:
I sent you all files inside pipeline. 2 of them are:

- profiles.py, where I have some predefined steps or command where we can call when run `custy run`. for example `custy run release`. As you can see the number of steps defined there. To log what happens it use decorators. to see it the logging level should be in specific level or the `--debug` should be true. The architecture is using basically mostly builder design pattern and pipeline design pattern.
- pipeline.py is where we run the pipeline

I can't send all the files related to pipeline because it s too many.

Please review it. Next is Related to command like `custy init`

=============

Related to the initalization or command like `custy init`, I send you these files.

You see many patterns, design patterns, architecture, etc

Please review it. Next is Related to hub tools or we can say like entrypoint related of these tools or scripts before.

=============

Related to tools as a hub for all those command. Or we can say maybe it is like the entrypoint or something:
I sent you:

- `app\core\workflow\workflow_builder.py`, where I think it is implements a builder design pattern
- `app\core\workflow\workflow_config.py`
- `app\core\workflow\workflow_engine.py`

Also additional related modules that I will sent. Mostly constants value related

- `app\constants\path.py`
- `app\constants\git_workflow_rules.py`
- `app\cli\constants\enums.py`

I don't really know what other design pattern that this code use. Because it is many type design and architecture so far.

Please review it. Next is Related to Additional files related to Makefile

============

And For additional things I send you my Makefile files.
Basically

- Makefile, is the entrypoint
- and all files inside `make/core` is where all related Makefile like for examples `.mk` files

I'm not send all the `.mk` files

I will just sent

- make\core\local\command.mk
- make\core\local\help.mk
- make\core\compose\command\common.mk
- make\core\compose\command\core.mk
- make\core\compose\help.mk
- make\core\remote\command\registry.mk
- make\core\remote\command\runtime.mk
- make\core\remote\help.mk

Also besides Makefile. I will sent you some metadata I have. Because it will be inside docker container or docker image. The application will be wrapped with docker especially docker compose

I will send

- Dockerfile
- docker-compose.yml
- docker-compose.dev.yml
- docker-compose.prod.yml
- .pre-commit-config.yaml

After this be ready. we can move to

1. UI / UX
2. Testing
3. Documentation
4. Metadata

============

Related to design patterns identified please remember we will also need some documentations related to design pattern. specific for that like the usage, where it used and implemented on the code. which files, class, methods, or functions, or whatever.

Done with the core files I sent you before.

Let's start these one by one

```
1. UI / UX
2. Testing
3. Documentation
4. Metadata
```

Let's start with frontend part. The UI / UX.

I will send my files first, the you just update the help message. commonly on `app/core/commands/**/command.py`.

What I sent for each of that folder have these folder that I will send

- command.py
- options.py
- models.py
- resolver.py

After I send those files one by one. Please review them then just update the help message. Make sure the UI / UX message still clear and detailed but not overwhelming but still proporsional and detail and clear. Use easy understand languages. Because it integrated with typer, rich, console, please use colors. Please see the `app\theme\theme.py` that I send you before related to color palette used.

I defined the steps for this:

1.1. `app/core/commands/init/command.py`
1.2. `app/core/commands/validate/command.py`
1.3. `app/core/commands/version/command.py`
1.4. `app/core/commands/backup/command.py`
1.5. `app/core/commands/cleanup/command.py`
1.6. `app/core/commands/changelog/command.py`
1.7. `app/core/commands/git_ops/command.py`
1.8. `app/core/commands/run/command.py`
1.9. `app/core/commands/workflow/command.py`
1.10. `app/core/commands/main/command.py` and `app/core/main.py`
1.11. Review changes

After these 11 steps until `1.11`. We start the step 2. Testing

Are you ready?

============

I send you these
app/cli/commands/init/command.py
app/cli/commands/init/options.py
app/cli/commands/init/models.py
app/cli/commands/init/resolver.py

===============

Currently is step is 1.11, Review changes.

I already reviewed. Now the UI and UX is a lot better.

Phase or step 1 (UI/UX) complete

so phase 1.11 is done.

After review changes, we start the step 2 (Testing)

do you already have plan for step 2 (Testing). Please I want to see the plan and review it before continue starting implementing.

===========

Sounds and Looks like a big plan

Alright we can go with the
recommendation.

Also in these starting step 2

Also please share the approximate list new files or file need to changes related to phase 2. So please share those and after you send I will review and if confirmed and we can start implementing

================

Wow so many files. I confirmed it and it is better to start the step now and sending those needed files one by one. Also make sure to use better documentation for each files and class and functions or methods

---

Wow so many files. So there are steps 2.1 until 2.15. I confirmed it and it is better to start the step now and sending those needed files. Also make sure to use better documentation for each files and class and functions or methods. Also due to many files related to this step, I think it is better to save space in this chatgpt space chat session send those files in a downloadable content instead of printing all of those code here. So just send all related files at once for each sub steps. For example I see step 2.1 is about foundation testing and approximately 14 files, then those 14 files send at once. and each of the end of these sub steps (for example if now is an end of step 2.1, then need review and approvement and start step 2.2), there wil be review content or files and if approved then continue. basically just review or give feedback. Do you agree and got it and understand? Please confirmed

# ==============================================

# ==============================================

# ==============================================

I need you to understand current Custy project I have. Please understand in detail. I will send the related files one by one. then I need to update some files. Mostly it is about UI, UX, testing, and documentations.
So priority or the order or the steps to update overall are:

1. UI and UX (Done)
2. Testing (Done)
3. Documentations
4. Meta data

We can skip steps 1 and 2, because it actually done. So just do the step 3 and 4.

Details:
For UI and UX, I will send all my files related to help message for CLI in `app/cli/commands`. mostly the file name is command.py. So I just need to update the UI and the UX. If the UI / UX is already good then it is okay. The UI it using CLI with rich, console, and other libraries. Just update the help message. Adjust to current command or files how it works or available commands, etc.

Then testing part, where we make a tests for those files. Idk it is good or not to have tests with 100% coverage. but let's see.

For documentation. I will follow like this structure

```
├── docs
│   ├── architecture (This is related to detailed docs or documentation about architecture projects)
│   │   ├── examples.md  (This document contains practical examples. Examples range from local testing to production publishing workflows. Or ther examples)
│   │   ├── faq.md (This document answers common questions about the command)
│   │   ├── overview.md (Overview related to the command)
│   │   ├── requirements.md (This is related to requirements need to run the apps or tools. but specifically the command or this command. For example some requirements.md they need the user to have git or docker installed. Some of them are not required anything and can just use apps or tools and run the command)
│   │   └── workflow.md (This document describes how the Dockerize workflow operates internally.)
│   ├── commands (This is documentation or docs related to commands available for the projects or tools)
│   │   ├── design-patterns.md  (This is related to design pattern used)
│   │   ├── diagrams.md (This is the diagrams related to this architecture)
│   │   └── workflow.md (This is related to architecture workflow)
│   ├── developer-guide (This is docs as guides and the are developers or among developers)
│   │   ├── blackbox (This is just reads or documentation how the project related to blackbox)
│   │   ├── tooling (This is just reads or documentation how the project related to some tools)
│   │   ├── developer-guide.md (This is docs as guides and the are developers or among developers. Overall docs or documentations)
│   │   ├── docker-workflow.md (This is related to how developers use docker or related to docker usage from the certain project)
│   │   └── getting-started.md (This is related to how developers getting started related to the certain project)
│   ├── diagrams (This is a collection of diagrams related to project. This can contain md files or documentation like architecture diagrams, workflow diagrams, activity diagrams, menu design diagrams, flowchart diagrams, algorithm diagrams, models diagrams, use case diagrams)
│   │   ├── generated
│   ├── project (This is related to project overall)
│   ├── reads (This is related other things just for reads or readings)
│   ├── testing (This is related to the documentation related to testing or test or testing or do some tests or do some testing to the project)
│   │   └── testing-guide.md
│   ├── user-guide (This is docs as guides and the target are users or any users)
│   │   ├── commands.md (Available commands the users can use)
│   │   ├── installation-methods.md (Installation method the users can use)
│   │   ├── lifecycle.md (Reads for users related to lifecycle usage)
│   │   ├── overview.md (Overview related to the project)
│   │   └── quickstart.md (Quick start guide for user related to usage)
│   ├── badges.md (Bagdes related to project)
│   ├── configuration.md (Configuration docs related to project)
│   ├── how-to-use.md (how-to-use docs related to project)
│   ├── index.md (Entrypoint docs related to project or just an index.md file)
│   ├── infrastructure.md (infrastructure docs related to project)
│   ├── installation.md (Installation related docs related to project)
│   ├── project_structure.md or PROJECT_STRUCTURE.md (project structure related to project. HINT: actually these files created or generated by doc-gen project by running doc-gen structure generate command when the tools or apps already installed or run by using docker image)
│   ├── TODO_tracking_history.md (This is just TODO files, but ignoreable)(For this no need to generate this one)
│   └── usage.md (This documentations or docs is the technical reference companion. Adcvanced references)
│   TODO.md
└── README.md (This README.md related to README.md from the root directory for the certain projects)
```

For Metadata to update there are

- pyproject.toml
- CONTRIBUTING.md
- SECURITY.md
- AGENTS.md
- .blackbox/skills/\*\*/SKILL.md

Are you ready?

=============

Also take a not the documentation should be on .md or .mdx format because I wiill put the documentation on docusaurus project. The docusaurus project, react js combined with typescript. Also for each .mdx file because it using docusaurus make sure it have metadata or something like

```

---

title: "Badges"

description: "🏷️ Project Badges"

sidebar_position: 3

sidebar_key: reflow

---

```

Some metadata or something like that on the top. The documentation should align with mdx since it use react js + typescript. When we start to making the docs I will give some references .mdx file for what I mean.

before go to the `app/cli` or the frontend part or side. I will sent all the backend logic workflow before go to the frontend to understand well.

So overall the flow is like

User -> CLI -> resolve the user input -> go to the core parts or call same class or functions or methods there. I can't explain everything because it is too complex and too many. instead I will send the files or related files one by on.

So we start from core or backend to the cli or frontend

Just as note. if made updates use better languages.

Got it?

=============

Let's start with Banner related

For banner it use Pyfiglet or figlet
I sent you:

- `app\services\banner_service.py`

Related to CLI theme
I sent you `app\theme\theme.py`

Please review it. Next is Related to configuration loader

=============

Related to configuration loader
I sent you:

- `app\config\config_loader.py`, is the loader script
- `config.toml`, is the file that being loaded. This is outside the app/ package. So the user just need have `config.toml` everywhere they want and use the ``app\config\config_loader.py`. So basically `config.toml` build to make the app more customizable and flexible. It is like a list of editable configuration based on user preferences

Related inside app/core/

Just remember all of them, including the plan related, UI, Tesing, Documentations, or meta data that will be updated. Remember the list of updated or new files based on documentation structure I send you before.

Related to Dryrun parameter options and logging or logger
I sent you:

- dry_run_support.py
- dry_run.py
- logging.py

Additionally I send you

- parsing.py

Please review it. Next is Related to UI tools inside `app/ui/`

=============

Related to UI tools or script for CLI
I sent you these files related to that

Also I give you my current CLI looks like and available overall command. The UI and UX for this CLI is already done.

Currently if I ran Custy

`````
(venv) D:\project\backups\git_commit\custy-2>custy

  .,-:::::  ...    ::: .::::::.::::::::::::.-:.     ::-.
,;;;'````'  ;;     ;;;;;;`    `;;;;;;;;'''' ';;.   ;;;;'
[[[        [['     [[['[==/[[[[,    [[        '[[,[[['
$$$        $$      $$$  '''    $    $$          c$$"
`88bo,__,o,88    .d888 88b    dP    88,       ,8P"`
  "YUMMMMMP""YmmMMMM""  "YMmMY"     MMM      mM"

                  v1.10.14.post1


 Usage: custy [OPTIONS] COMMAND [ARGS]...

 🚀 Custy — Release Automation Platform

 Automate versioning, changelog generation, release workflows,
 Git operations, project initialization, validation, backups,
 cleanup tasks, and workflow orchestration.

 Built for consistent releases, automation, and developer productivity.

 ────────────────────────────────────────

 ⚡ Quick Start

 1. Initialize your project

    custy init

 2. Validate your setup

    custy validate

 3. Run your first workflow

    custy run release

 ────────────────────────────────────────

 🚀 Recommended Commands

 custy run dev

`````

Daily development workflow

commit → push

```

custy run release

```

Standard release workflow

commit → tag → push

```

custy run full

```

Complete release lifecycle

release → changelog → backup → cleanup

```

────────────────────────────────────────

📦 Core Commands

custy init
Initialize configuration, templates, and examples

custy validate
Validate project readiness

custy commit
Create a structured commit

custy tag
Create release tags

custy push
Push commits and tags

custy run
Execute workflow pipelines

────────────────────────────────────────

🧩 Release Management

custy version
Synchronize project versions

custy changelog
Generate release notes and CHANGELOG.md

────────────────────────────────────────

🛠️ Project Maintenance

custy backup
Backup message templates

custy cleanup
Remove stale backups and branches

────────────────────────────────────────

⚙️ Advanced Features

custy workflow

```

Branch and release workflow management

⚠ Experimental / Beta

```

────────────────────────────────────────

🌐 Global Options

Global options are specified before commands.

custy --dry-run run release

custy --debug commit

custy --log-level debug tag

────────────────────────────────────────

❓ Need Help?

View command help:

```

custy run --help

custy tag --help

custy workflow --help

```

────────────────────────────────────────

💡 Tip

Most users should use:

```

custy run release

```

instead of executing commit, tag, and push manually.

╭─ Options ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ --no-banner                     Disable startup banner                                                                                                                          │
│                                 Hide the Custy banner during command execution.                                                                                                 │
│                                 Useful for:                                                                                                                                     │
│                                 • CI/CD environments • Automated scripts • Cleaner console output                                                                               │
│                                 Default: False                                                                                                                                  │
│ --help                -h        Show help                                                                                                                                       │
│ --version             -v        Display the installed Custy version and exit.                                                                                                   │
│ --install-completion            Install completion for the current shell.                                                                                                       │
│ --show-completion               Show completion for the current shell, to copy it or customize the installation.                                                                │
╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Global • Execution ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ --dry-run  -dr  --no-dry-run  -Dr    Dry run mode                                                                                                                               │
│                                      Simulate execution without modifying files, repositories, versions, tags, branches, or remote systems.                                     │
│                                      Useful for:                                                                                                                                │
│                                      • Testing workflows • Verifying release pipelines • Debugging command execution                                                            │
│                                      ✔ No changes are applied.                                                                                                                  │
│                                      Default: False                                                                                                                             │
╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Global • Debug ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ --debug      -dbg  --no-debug  -Dbg                                       Debug mode                                                                                            │
│                                                                           Enable detailed diagnostic output.                                                                    │
│                                                                           Useful when troubleshooting:                                                                          │
│                                                                           • Workflow execution • Pipeline behavior • Configuration issues • Validation failures                 │
│ --log-level  -ll                     [critical|error|warning|info|debug]  Logging level                                                                                         │
│                                                                           Control logging verbosity.                                                                            │
│                                                                           • critical Critical errors only                                                                       │
│                                                                           • error Errors only                                                                                   │
│                                                                           • warning Warnings and errors                                                                         │
│                                                                           • info General information (default)                                                                  │
│                                                                           • debug Detailed diagnostics                                                                          │
│                                                                           Default: info                                                                                         │
│                                                                           HINT: Prefer configuring this in config.toml.                                                         │
│                                                                           Variable: 'tool.custy.logging.level'                                                                  │
╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ init       Initialize a project for Custy                                                                                                                                       │
│ validate   Validate project readiness                                                                                                                                           │
│ commit     Create a commit                                                                                                                                                      │
│ tag        Create a version tag                                                                                                                                                 │
│ push       Push changes to remote repositories                                                                                                                                  │
│ run        Execute workflow pipelines                                                                                                                                           │
│ changelog  Changelog generation                                                                                                                                                 │
│ version    Version synchronization                                                                                                                                              │
│ backup     Backup template files                                                                                                                                                │
│ cleanup    Cleanup project resources                                                                                                                                            │
╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ (Still in development) ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ workflow   Git workflow transitions                                                                                                                                             │
╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯


(venv) D:\project\backups\git_commit\custy-2>
```

As you can see what CLI looks.

For thge details inside app/cli/ I will send relatable files later. This is just and overview.

Also I send you the screenshot when I run the app.

The next is related to core logic for those specific commands. NOTES that Please understand the custy.

=============

For the logic part. I will send the core tools. For the details backend or core and frontend or the CLI related to each command. I will send alongside when we start creating the documentation.

##### core tools

Related to tools as a hub for all those command. Or we can say maybe it is like the entrypoint or something:

I send these 3 files on this chatgpt project sources. Please confirm if you received well

I sent you:

- `app\core\workflow\workflow_builder.py`, where I think it is implements a builder design pattern
- `app\core\workflow\workflow_config.py`
- `app\core\workflow\workflow_engine.py`

Also additional related modules that I will sent. Mostly constants value related.

For these additional 6 files I will send the files here. Please confirm if you received well

- `app\constants\path.py`
- `app\constants\git_workflow_rules.py`
- `app\constants\resolver.py`
- `app\cli\constants\enums.py`
- `app\cli\constants\completions.py`
- `app\cli\constants\args.py`

I send these 6 files on this chatgpt project sources. Please confirm if you recieved well

I don't really know what other design pattern that this code use. Because it is many type design and architecture so far.

# Design Patterns Identified So Far

Custy currently contains at least:

| Pattern                    | Usage                                               |
| -------------------------- | --------------------------------------------------- |
| Strategy                   | Version strategies                                  |
| Builder                    | WorkflowEngineBuilder, InitBuilder, PipelineBuilder |
| Factory                    | GitService Factory                                  |
| Registry                   | StepRegistry, Makefile registry                     |
| Pipeline                   | Pipeline execution                                  |
| Decorator                  | log_step                                            |
| Facade                     | WorkflowEngine                                      |
| Presenter                  | InitializationPresenter                             |
| DTO                        | WorkflowConfig, InitSpec                            |
| Context Object             | GitContext                                          |
| Adapter                    | CLI → WorkflowConfig                                |
| Service Layer              | GitService, WorkflowEngine                          |
| Template Method Style      | Init workflow                                       |
| Protocol-based Abstraction | Git protocol, TagStrategy                           |

Related to design patterns identified please remember we will also need some documentations related to design pattern. specific for that like the usage, where it used and implemented on the code. which files, class, methods, or functions, or whatever.

Please review it. Next is Related to Additional files related to Makefile

============

I send again the
`app/cli/constants/enums.py`

Also additional files inside `app/ui/`

- `app\ui\banner.py`
- `app\ui\console.py`
- `app\ui\exceptions.py`
- `app\ui\penels.py`
- `app\ui\tables.py`

##### Addition

And For additional things I send you my Makefile files.
Basically

- Makefile, is the entrypoint
- and all files inside `make/core` is where all related Makefile like for examples `.mk` files

I'm not send all the `.mk` files

I will just sent

- make\core\local\command.mk
- make\core\local\help.mk
- make\core\compose\command\common.mk
- make\core\compose\command\core.mk
- make\core\compose\help.mk
- make\core\remote\command\registry.mk
- make\core\remote\command\runtime.mk
- make\core\remote\help.mk

Also besides Makefile. I will sent you some metadata I have. Because it will be inside docker container or docker image. The application will be wrapped with docker especially docker compose

I will send

- `Dockerfile`
- `docker-compose.yml`
- `docker-compose.dev.yml`
- `docker-compose.prod.yml`
- `.pre-commit-config.yaml`

=============

Related to "UI Layer" part
You say
"(I noticed your message says penels.py; the uploaded file is panels.py.)". Actuaally I type. the actual name is panel.py not penel.py. It from `app/ui/panels.py`

I will send:

- My current `pyproject.toml` but need update on steps 2, "Metadata"

After this be ready. we can move to

1. Documentation
2. Metadata

============

So as note for step `1. Documentation`

I send the examples of .mdx file from docusaurus project

- docs/reflow/commands/dockerize/_category_.json
- docs/reflow/commands/dockerize/examples.mdx
- docs/reflow/commands/dockerize/faq.mdx
- docs/reflow/commands/dockerize/overview.mdx
- docs/reflow/commands/dockerize/requirements.mdx
- docs/reflow/commands/dockerize/workflow.mdx

but the content of the .mdx example currently not really alligned with docusaurus. It is just an example. Related to custy the .mdx need to improve. maybe we need to create some react components or whatever to make the docusaurus documentation looks better.

Please review the example of .mdx and let me know if we can improve the UI or UX, maybe inside .mdx file or use other new component. Remember it use docusaurus + react js + typescript. for docusaurus project.

This is color palette I use on my docusaurus project

```
##### Overall light theme and dark theme

| Light Theme            | Dark Theme             |
| ---------------------- | ---------------------- |
| #FFFFFF Background     | #0B1220 Background     |
| #F8FAFC Surface        | #111827 Surface        |
| #F1F5F9 Card           | #1A2332 Card           |
| #99015B Primary        | #D81B85 Primary        |
| #3FCAFF Secondary      | #4DD9FF Secondary      |
| #1B3569 Accent         | #3A7BFF Accent         |
| #014C68 Tertiary       | #1B3569 Tertiary       |
| #013F56 Hover          | #24518D Hover          |
| #0F172A Text Primary   | #F8FAFC Text Primary   |
| #475569 Text Secondary | #CBD5E1 Text Secondary |
| #CBD5E1 Border         | #2B3648 Border         |
```

The docusaurus project that I have now is like

```
the repository docusaurus documentation project centralizes documentation for multiple projects into a single, structured, maintainable documentation website with multilingual support.
```

So it collection of documentations from different and mostly all my project I made. Currently on docusaurus project, I have documentation for 3 projects. i.e. `doc-gen`, `path-header-scanner`, `reflow`

and current structure on docusaurus project mostly like

```
├── docs
│   ├── `doc-gen`
│   ├── `path-header-scanner`
│   └── `reflow`
└── ...(etc)
```

currently no docs for custy, later it becomes like

```
├── docs
│   ├── `doc-gen`
│   ├── `path-header-scanner`
│   ├── `reflow`
│   └── `custy` <- (NEW ADDED HERE, ALL RELATED DOCUMENTATION FOR CUSTY PROJECTS OR TOOLS OR APPS)
└── ...
```

Also I will send README.md from docusaurus project. Note that I will send README.md from docusaurus project not from custy project to understand also the documentation docusarus project

So along side we create the .mdx file. For each documentation.
1.1. Documentation based on `app/cli/` based on my custy project
1.2. Relatable documentation besides from `app/cli/`
1.3. Other documentation if needed

So for step `1.1.`. Each command we start creating.

- I send relatable files for each command relatable to frontend part or the UI / UX part. commonly on `app/core/commands/**/**.py`. What I sent for each of that folder have these folder that I will send.`command.py`, `options.py`, `models.py`, `resolver.py`, etc.
- I send relatbale files for each command relatable to backend part or the core logic part.
- I will have the screenshot for every command run. It could more than 1 screenshots. and I'll send it. and I'll explain it

After I send, please review it first and starting making the .mdx file and send the file one by one with better easy language. The target user for this custy project are devops, programmers, coders, software engineer. But even that mostly of them know coding or programming, still use the easy understand language. If there is a words or phrases related to coding or programming that not all understand well, if need to explain, then explain.

##### Summary

So the steps mostly like

Step 1.0. is an external or internal component if needed
Step 1.1. Documentation based on `app/cli/` based on my custy project
Step 1.2. Relatable documentation besides from `app/cli/`
Step 1.3. Other documentation if needed

and all sub steps will follow.

Are these understandable?

=============

Alright! First I thought we can first start with init command with something like `custy init` because I think it make sens to start with. But we can also run `custy run init`, both run the same, but with run command we can do `custy run commit tag push` or `custy run release` instead of `custy commit` + `custy tag` + `custy push`. I think it is better to me to just give core logic for pipeline first.

I send these11 files on this chatgpt project sources. These files related Alright! First I thought we can first start with init command with something like `custy init` because I think it make sens to start with. But we can also run `custy run init`, both run the same, but with run command we can do `custy run commit tag push` or `custy run release` instead of `custy commit` + `custy tag` + `custy push`. I think it is better to me to just give core logic for pipeline first.

I send these11 files on this chatgpt project sources. These files related to pipline core logic. To see the list of `custy run` see the `app\core\pipeline\profiles.py`. for example `custy dev`. Please confirm if you received well

- app\core\pipeline\builder.py
- app\core\pipeline\command_resolver.py
- app\core\pipeline\context.py
- app\core\pipeline\pipeline.py
- app\core\pipeline\profiles.py
- app\core\pipeline\progress.py
- app\core\pipeline\registry.py
- app\core\pipeline\step_registry.py
- app\core\pipeline\decorators\log_step.py
- app\core\pipeline\steps\base_step.py
- app\core\pipeline\steps\initialization\init_step.py

Please review first. We make docs later for this. After review we need to start with the init commands

=============

Wait! also before move on one more thing, since you say "need to create a small foundation with some components". Currently I have a homepage for my docusaurus project. I will send it now. It is on docusaurus project on `\src\components\homepage\`
These files like `\src\components\homepage\**.module.css`, `\src\components\homepage\**.tsx`. Also I send `devalltect00-docs\src\css\custom.css`, `src\data\projects.ts`, `src\pages\index.module.css`, `src\pages\index.tsx`

I just send these as references that currently my homepage like this.

Please review. After review we start with the init commands

=============

Wait Before send the init, I send the entry point on CLI side

CLI part on `app\cli\commands\main\`
app\cli\main.py
app\cli\commands\main\command.py
app\cli\commands\main\models.py
app\cli\commands\main\options.py
app\cli\commands\main\resolver.py

=============

Also please review the docs for each files to understand well.

Start with init command.

##### `custy init`

Related to the initalization or command like `custy init`, I send you these files.

CLI part:
app\cli\commands\init\command.py
app\cli\commands\init\models.py
app\cli\commands\init\options.py
app\cli\commands\init\resolver.py

Core part:
app\core\initialize\registry.py
app\core\initialize\main.py
app\core\initialize\loader.py
app\core\initialize\services\scaffold_generator.py
app\core\initialize\presenters\initialization_presenter.py
app\core\initialize\builder\init_builder.py
app\core\initialize\models\init_config.py
app\core\initialize\models\init_spec.py
app\core\initialize\models\initialization_result.py
app\core\initialize\models\template_dir.py
app\core\initialize\models\template_file.py

Please review it

=============

##### `custy changelog`

(7 files)

Related to command like `custy commit`, `custy tag`, `custy commit tag`, `custy push`, `custy commit push`, `custy commit tag`, `custy dev`, `custy release`, `custy full`. etc
I sent these files. I can't sent all the files because it is too many. But I explain a bit.

Start by command like `custy version update` and `custy changelog generate`

Related to update files. for example when run `custy version update`
I sent you:

- `app\core\files\update_files.py`

Related to generate changelog like CHANGELOG.md file. for example when run `custy changelog generate`

- The `app\core\git_ops\versioning\builder\release_builder.py` basically script to generate changelog. For example CHANGELOG.md. The related files like `app\core\git_ops\versioning\models\release_info.py` and `app\core\git_ops\versioning\models\version_type.py`. And also `changelog.j2` I send is the templates used
- The `app\core\git_ops\versioning\version_bridge.py`, basically a bridge among different tag format startegy
- `app\core\git_ops\commit\validator.py` Is where it validate the commit message. the commit message should follow specific formats defined.

=============

##### `custy run`, git_ops

5 (files)

Next is

Related to git Operations, I sent you these

- `app\core\git_ops\git\executor.py`
- `app\core\git_ops\git\factory.py`
- `app\core\git_ops\git\protocol.py`
- `app\core\git_ops\git\result.py`
- `app\core\git_ops\git\service.py`

As you can see how it being architect and designed. As you see the layers, there are many layers.

Please review it. Next is Related to tag strategy

=============

##### `custy tag`, `custy version`

(6 files)

Related to the tag strategy, I send you these files. I think it use the strategy design pattern, right?

Please review it. Next is Related to git operations helper

=============

##### `custy run`, git_ops

Related to git operations helper, I send you these files:

- `app\core\git_ops\helper\commitizen.py`
- `app\core\git_ops\helper\pep440_helper.py`
- `app\core\git_ops\helper\project_detector.py`
- `app\core\git_ops\helper\semver_helper.py`
- `app\core\git_ops\helper\version_helper_base.py`
- `app\core\git_ops\helper\version_utils.py`

Please review it. Next is Related to command like `custy backup commit` and `custy backup tag` and `custy backup all` and `custy cleanup backups` and `custy cleanup branches` and and `custy cleanup all`

=============

##### `custy backup`, `custy cleanup`

(4files)

Related to backup command:
I sent you:

- `app\core\backup\backup_manager.py`

Related to cleanup command:
I sent you:

- `app\core\cleanup\branch_cleaner.py`
- `app\core\cleanup\handle_cleanup_backups.py`
- `app\core\cleanup\handle_cleanup_branches.py`

Also Additionally to command `custy workflow`. Note that this is still in development. or the beta version. Actually the script is done for now. but still need more review before become the relase. So remember make sure to add this warning on CLI help message for `custy workflow` on the `--help` part

Related to workflow command (Still in development. or the beta version):
I sent you:

- `app\core\branch_workflow\branch_workflow_manager.py`

Please review it. Next is Related to command like `custy run`

=============

##### `custy run`

Related to run command or pipeline architecture part:
I sent you all files inside pipeline. 2 of them are:

- profiles.py, where I have some predefined steps or command where we can call when run `custy run`. for example `custy run release`. As you can see the number of steps defined there. To log what happens it use decorators. to see it the logging level should be in specific level or the `--debug` should be true. The architecture is using basically mostly builder design pattern and pipeline design pattern.
- pipeline.py is where we run the pipeline

I can't send all the files related to pipeline because it s too many.

Please review it. Next is Related to command like `custy init`

=============

=====

Each command

- mdx
- screenshot

===================

Related to decisions, you say `We document behavior, not implementation`. Yes we can do it. We can seperate the `commands/` and/from `architecture/` or `developer-guide` or `user-guide` sections.

Now we ready to begin.

Wait Before send the init, I send the entry point on CLI side

CLI part on `app\cli\commands\main\`
app\cli\main.py
app\cli\commands\main\command.py
app\cli\commands\main\models.py
app\cli\commands\main\options.py
app\cli\commands\main\resolver.py

Next I will send the relevant files for `custy init` command.

===================

I send the relevant files for `custy init` command.

the `custy init` command

Start with init command.

##### `custy init`

Related to the initalization or command like `custy init`, I send you these files.

CLI part:
app\cli\commands\init\command.py
app\cli\commands\init\models.py
app\cli\commands\init\options.py
app\cli\commands\init\resolver.py

Core part:
app\core\initialize\registry.py
app\core\initialize\main.py
app\core\initialize\loader.py
app\core\initialize\services\scaffold_generator.py
app\core\initialize\presenters\initialization_presenter.py
app\core\initialize\builder\init_builder.py
app\core\initialize\models\init_config.py
app\core\initialize\models\init_spec.py
app\core\initialize\models\initialization_result.py
app\core\initialize\models\template_dir.py
app\core\initialize\models\template_file.py

Continue

===================

I will answer your questions related to `custy init`

Question: What is this command?
Answer: Initializes a project for use with Custy.

Question: When should I use it?
Answer: It is recommended when starting using any Custy commands or Before using other Custy workflows, or when adding Custy to an existing project. Think of it tools like npm or yarn or whatever tools. if we run npm start it will create package.json. It is recommended to do that. But if the question is "Is It must use?", then the answer is no because we can manually create package.json and fill the files based on user's needs. But still it is highly recommended. In my case with Custy tools, if the question is "Is It must use?", then the answer is no because Custy by default have their default configurations on their system or code or programs or project or whatever it called. But still it is highly recommended because when run `custy init` or `custy run init`, it will create some folders and files under `.config/custy/`. And those files contains configurations and changeable. It will create more flexibility, customization, profiling, or preferences based on User's needs. For example, If they want to always saved the logged into the files with custom name, they can change the `.config/custy/config.toml` that is generated by running `custy init` or `custy run init`. On `.config/custy/config.toml`, specifically on `log_dir = "logs"` and `log_file = "custy.log"` sections, by default it will saved under `logs/custy.log`. But if the user want to user custom name for example `app.log` then on `.config/custy/config.toml` file, change `log_file = "custy.log"` section to `log_file = "app.log"`. So the logged will saved under `logs/app.log` instead of `logs/custy.log`. I send you the example of `config.toml` that generated when we run `custy init` or `custy run init`

Question: What does it change?
Answer: Creates configuration files, templates, version file, and optionally example resources. Adding those folders to the target user's project under `.config/custy/` folder. So when they run `custy init` or `custy run init`, They will have this kind of folder or this structure

```
.(Any of user's project where they run the `custy init` or the `custy run init`)
├── .config
│   ├── custy
│   │   ├── templates
│   │   │   ├── backups
│   │   │   │   ├── commit
│   │   │   │   └── tag
│   │   │   ├── changelog
│   │   │   │   └── changelog.j2
│   │   │   ├── examples
│   │   │   │   ├── commit_message
│   │   │   │   └── tag_message
│   │   │   ├── commit-message.txt
│   │   │   └── tag-message.txt
│   │   └── config.toml
```

Question: What doesn't it do?
Answer: It does not execute any core commands or release workflows or modify your Git history any operations.

Question: What's next?
Answer: The next command is depends on user needs and all optionally. If they want to commit they can run Run `custy commit` or `custy run commit`. If they want to commit, tag, push they can run Run `custy run release` or `custy run commit tag push`. For details. I send you the `profiles.py` to see list of commands when we run `custy run`

If no questions, then please proceed

===================

Very good if you really understand. Just one note. one .mdx file don't forget to add some meta information like for example

```
---
title: "Developer Guide"
description: "Developer Guide"
sidebar_key: reflow-developer-guide
... (other needed information)
---
```

or

```
---
title: "Design Patterns"
description: "This document explains design patterns used in the DocGen project."
sidebar_position: 1
sidebar_key: doc-gen-architecture
... (other needed information)
---
```

Note that they `sidebar_key` properties's value, the `reflow-developer-guide` and `doc-gen-architecture`. They start with `reflow` and `doc-gen`. it indicates the name of the project documentation they pointing to.

So in our case on Custy tools. the `sidebar_key` properties's value should start with `custy-` or something like that.

Alright I'm ready. Please proceed

===================

Of course. We already agree "Finish the roadmap before adding new work."

Now as requested for the validate method

I send you

```
app/cli/commands/validate/
    command.py
    models.py
    options.py
    resolver.py
```

The command can `custy validate` or `custy run validate`

For the command `custy validate` command
please check the `profiles.py` that already I send on This project source'e ChatGPT session`.
This file related to Custy project itself not docusaurus project documentation.
The steps like

```
"validate": [
        {"name": "ensure_git_repo_step"},
        {"name": "ensure_remote_exists_step"},
        {"name": "ensure_version_file_step"},
        {"name": "ensure_staged_changes_step"},
        {"name": "ensure_commitizen_convention_step"},
        {"name": "ensure_commit_message_file_step"},
        {"name": "ensure_commit_message_file_exists_step"},
        {"name": "ensure_tag_message_file_step"},
        {"name": "ensure_tag_message_file_exists_step"},
    ],
```

For the command `custy run validate` command
please check the `workflow_engine.py`, `workflow_builder.py`, `workflow_config.py`, that already I send on This project source's ChatGPT session`.
The steps like

```
    # =========================================================
    # Public Validation Entry
    # =========================================================
    def validate(self) -> None:
        """
        Run all validation steps required before executing workflow.

        Order matters:
        1. Environment checks
        2. File checks
        3. Git state checks
        """

        self.ensure_git_repo()
        self.ensure_remote_exists()
        self.ensure_version_file()
        self.ensure_commit_message_file()
        self.ensure_tag_message_file()
        self.ensure_staged_changes()
        self.ensure_commitizen_convention()
        self.ensure_commit_message_file_exists()
        self.ensure_tag_message_file_exists()

    # =========================================================
    # Validation Steps
    # =========================================================

    def ensure_git_repo(self):
        """
        Ensure current directory is a Git repository.
        """
        if not self.gitService.is_git_repo():
            raise ValidationError(
                message="Git repository not found.",
                hint="Run `git init`",
                code="NOT_GIT_REPO"
            )

    def ensure_remote_exists(self, remote: str = "origin"):
        """
        Ensure a required Git remote exists.

        Args:
            remote (str): Remote name to check (default: 'origin').

        Behavior:
            - Uses GitService abstraction
            - Can be skipped via --skip-checks
        """

        if self.skip_checks:
            return

        if not self.gitService.check_remote(remote):
            raise ValidationError(
                message=f"Git remote '{remote}' not found or unreachable.",
                hint=f"Add remote using: git remote add {remote} <url>",
                code="REMOTE_NOT_FOUND",
                context={"remote": remote}
            )

    def ensure_version_file(self):
        """
        Ensure version file exists.
        """

        # print("self.version_file", self.version_file)

        if not self.version_file or not self.version_file.exists():
            raise ValidationError(
                message=f"Version file '{self.version_file}' not found.",
                hint="Run `custy init config`",
                code="VERSION_FILE_NOT_FOUND"
            )

    def ensure_commit_message_file(self):
        """
        Ensure commit message file exists.
        """
        if not self.commit_message_file or not self.commit_message_file.exists():
            raise ValidationError(
                message=f"Commit message file '{self.commit_message_file}' not found.",
                hint="Run `custy init config`",
                code="COMMIT_MSG_FILE_NOT_FOUND"
            )

    def ensure_tag_message_file(self):
        """
        Ensure tag message file exists.
        """
        if not self.tag_message_file or not self.tag_message_file.exists():
            raise ValidationError(
                message=f"Tag message file '{self.tag_message_file}' not found.",
                hint="Run `custy init config`",
                code="TAG_MSG_FILE_NOT_FOUND"
            )

    def ensure_staged_changes(self):
        """
        Ensure there are staged changes before committing.

        Behavior:
        - Checks via GitService
        - Supports auto-stage
        - Supports force commit
        """

        if self.gitService.has_staged_files():
            return

        print("self.auto_stage===========",self.auto_stage)

        # ===== Auto-stage =====
        if self.auto_stage:
            logger.warning("⚠️ No staged changes detected. Auto staging...")
            logger.debug("📦 Auto-running: git add .")

            if self.stage_mode == StageModeChoices.UPDATE:
                self.gitService.auto_stage_update()
            else:
                self.gitService.auto_stage_all()

            if not self.dry_run:
                if not self.gitService.has_staged_files():
                    logger.debug("⚠️ Still no staged files after auto-staging. but proceeding due to --force-commit.")
                    if self.force_commit:
                        logger.warning("⚠️ Proceeding due to --force-commit")
                        return

                    logger.debug("⚠️ [yellow]Warning[/yellow]: Still no staged changes after `git add .`")
                    confirm = input("No staged files. Continue? (y/n): ").strip().lower()
                    if confirm not in ["y", "yes"]:
                        raise ValidationError(
                            message="Aborted due to empty staging.",
                            hint="Stage files using `git add`",
                            code="EMPTY_STAGING_ABORTED"
                        )
                    logger.debug("⚠️ Continuing despite no staged. files.")

            staged_files = self.gitService.list_staged_files()

            if staged_files:
                logger.info("[cyan]📝 Files staged:[/cyan]")
                for f in staged_files:
                    logger.info(f"[green]  - {f}[/green]")
            else:
                logger.debug("⚠️ No files were staged.")
                if self.force_commit:
                    logger.warning("⚠️ Proceeding with empty commit (--force-commit)")
                    return

                raise ValidationError(
                    message="No files staged after auto-stage.",
                    hint="Check your working directory",
                    code="AUTO_STAGE_FAILED"
                )

        elif self.force_commit:
            logger.warning("⚠️ No staged files but continuing (--force-commit)")
            return

        else:
            if self.dry_run:
                logger.warning(
                    "[yellow](dry-run)[/yellow] No staged changes detected. "
                    "Would normally fail with NO_STAGED_CHANGES.\n"
                    "Hint: Use `git add .` or enable --auto-stage."
                )
                return
            raise ValidationError(
                message="No staged changes to commit.",
                hint="Use `git add .`",
                code="NO_STAGED_CHANGES"
            )

    def ensure_commitizen_convention(self):
        """
        Ensure commitizen convention.
        """
        if not self.commitizenHelper.check_commit:
            raise ValidationError(
                message=f"Checking commitizen convention '{self.commit_message_file}' not found.",
                hint="Use cz check --commit-msg-file (path)]",
                code="COMMITIZEN_CONVENTION_FAILED"
            )

    def ensure_commit_message_file_exists(self) -> None:
        """
        Ensure commit message file exists before editing.

        This file is required for:
        - commit message generation
        - user editing phase

        Raises:
            ValidationError: If file is missing.
        """

        if not self.commit_message_file:
            raise ValidationError(
                message="Commit message file path is not provided.",
                hint="Use --message-file or run `custy init templates`.",
                code="COMMIT_MSG_PATH_MISSING",
            )

        if not self.commit_message_file.exists():
            raise ValidationError(
                message=f"Commit message file not found: {self.commit_message_file}",
                hint="Run `custy init templates` to initialize required files.",
                code="COMMIT_MSG_FILE_NOT_FOUND",
                context={"path": str(self.commit_message_file)},
            )

    def ensure_tag_message_file_exists(self) -> None:
        """
        Ensure tag message file exists before editing.

        Raises:
            ValidationError: If file is missing.
        """

        if not self.tag_message_file:
            raise ValidationError(
                message="Tag message file path is not provided.",
                hint="Use --tag-message-file or run `custy init templates`.",
                code="TAG_MSG_PATH_MISSING",
            )

        if not self.tag_message_file.exists():
            raise ValidationError(
                message=f"Tag message file not found: {self.tag_message_file}",
                hint="Run `custy init templates` to initialize required files.",
                code="TAG_MSG_FILE_NOT_FOUND",
                context={"path": str(self.tag_message_file)},
            )
```

I will explain a bit. Basically The User that initialize the custy with `custy init` command, on their own project coding. Let's say the name of the User's project we say 'projA' as alias

And all these configuration can be setting on config.toml by the user if they run `custy init` or they already have that file already

==================

- "ensure_git_repo_step"
  Basically like make sure is the repo already exist or initialize on the project 'projA' coding.

Check `ensure_git_repo_step` method on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside that method

- "ensure_remote_exists_step"
  If on config.toml we already defined the remote name, make sure is already exist for example 'origin' or 'upstream' or 'backup' or 'backups' or any name that we already defined on config.toml. Are the defined remote alread defined on the git repo the User's owned on the project 'projA' coding.

Check `ensure_remote_exists_step` method on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside that method

- "ensure_version_file_step"
  Make sure the project 'projA' coding has file like "**version**.py" or something like that. The specific file name or the location see the config.toml configuratiion. You know to indicate the User's project version. For example version 1.2.3

Check `ensure_version_file_step` method on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside that method

- "ensure_staged_changes_step"
  Check if already staged. command like `git add` or `git add .` or `git add [some file]`. Is there already some files that already been staged or not

Check `ensure_staged_changes_step` method on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside that method

- "ensure_commitizen_convention_step"
  This one optionally. If the user use commitizen convention tag strategy instead of using SemVer or PEP440 or other the tag strategy that Custy had offer to use.
  The list of TagStrategy are

```
"CommitizenStrategy", -> Using commitizen convention to generate the tag version
"DateStrategy", -> Using date tag strategy to generate the tag version. For example if the project repo currently has 10 commits and the tag created at "2023.08.10", the tag version will be "2023.08.10". maybe later Custy Update or future release update it will become "v2023.08.10" or "v2023.08.10.1" or "v2023.08.10.2" or "v2023.08.10.3" and so on.
"GitCountStrategy", -> Using Git count tag strategy to generate the tag version. Basically the tag version based on the number of commits on the project repo. For example if the project repo currently has 10 commits, the tag version will be v10. maybe later Custy Update or future release update it will  become 0.0.10. I still consider to use this to count based on number of commit on the repo or number of tag already exist on the repo. I will decide later. But for now I will use the number of commits on the repo to generate the tag version.
"PEP440Strategy", -> Using PEP440 convention to generate the tag version
"SemverStrategy", -> Using SemVer convention to generate the tag version
```

Check `ensure_commitizen_convention` method on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside that method

But so far personally as a Custy Developer, I prefer use SemVer or PEP440 convention to generate the tag version. Or specifically SemVer because Github or Gitlab commonly recognize SemVer convention. But if the User want to use other convention, they can set it on config.toml file.

- "ensure_commit_message_file_step"
- "ensure_commit_message_file_exists_step"

This two part to check if file ".config/custy/templates/commit-message.txt" that already generated using `custy init templates` command.

Check `ensure_commit_message_file` and `ensure_commit_message_file_exists` methods on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside those method

- "ensure_tag_message_file_step"
- "ensure_tag_message_file_exists_step"

This two part to check if file ".config/custy/templates/tag-message.txt" that already generated using `custy init templates` command.

Check `ensure_tag_message_file` and `ensure_tag_message_file_exists` methods on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside those method

and the

- "validate"

Run all those validaton steps

==================

For references. To see and understand what actually class like `self.gitService`. and the method for example `self.gitService.is_git_repo()`.
To see how they works, please check these 5 files I already attached on This project source's ChatGPT session`. Especially `app/core/git_ops/git/executor.py`
These 5 files related to Custy project itself not docusaurus project documentation. These 5 files are:

```
app/core/git_ops/git/executor.py
app/core/git_ops/git/factory.py
app/core/git_ops/git/protocol.py
app/core/git_ops/git/result.py
app/core/git_ops/git/service.py
```

These commands `custy validate` or `custy run validate` can be run before we want to commit, tag, push. Whether using Custy or just just git tool. But seems before doing some of the Custy workflow

But if you see profiles.py, when we do something like `custy run` or `custy run commit` or `custy run commit tag push` or `custy release` or or `custy full` or whatever they are defined on profiles.py, mostly and seems always run validate at start.
But not if just `custy commit`, `custy tag`, etc the custy command not with run like `custy run`. this command `custy commit` just doing commit. but if we run using `custy run` see the profiles.py. It always need to validate

So basically these commands `custy validate` or `custy run validate` is optionally to run. Because most of the `custy run` workflow run the validate
but not using `custy run` command, then optionally to run `custy validate` or `custy run validate`

Basically It just validate, ensure, make sure, check, verify, confirm, and so on. Before we do the next step like commit, tag, push, release, etc. But if we run using `custy run` command, it will automatically run validate first before doing the next step.

Actually for current step is `custy validate` command. and not yet with `custy run` command. This is just some overviews.

Is there any questions so far?

===================

Of course. We already agree "Finish the roadmap before adding new work."

Now as requested for the validate method

I send you

```
app/cli/commands/validate/
    command.py
    models.py
    options.py
    resolver.py
```

The command can `custy validate` or `custy run validate`

For the command `custy validate` command
please check the `profiles.py` that already I send on This project source'e ChatGPT session`.
This file related to Custy project itself not docusaurus project documentation.
The steps like

```
"validate": [
        {"name": "ensure_git_repo_step"},
        {"name": "ensure_remote_exists_step"},
        {"name": "ensure_version_file_step"},
        {"name": "ensure_staged_changes_step"},
        {"name": "ensure_commitizen_convention_step"},
        {"name": "ensure_commit_message_file_step"},
        {"name": "ensure_commit_message_file_exists_step"},
        {"name": "ensure_tag_message_file_step"},
        {"name": "ensure_tag_message_file_exists_step"},
    ],
```

For the command `custy run validate` command
please check the `workflow_engine.py`, `workflow_builder.py`, `workflow_config.py`, that already I send on This project source's ChatGPT session`.
The steps like

```
    # =========================================================
    # Public Validation Entry
    # =========================================================
    def validate(self) -> None:
        """
        Run all validation steps required before executing workflow.

        Order matters:
        1. Environment checks
        2. File checks
        3. Git state checks
        """

        self.ensure_git_repo()
        self.ensure_remote_exists()
        self.ensure_version_file()
        self.ensure_commit_message_file()
        self.ensure_tag_message_file()
        self.ensure_staged_changes()
        self.ensure_commitizen_convention()
        self.ensure_commit_message_file_exists()
        self.ensure_tag_message_file_exists()

    # =========================================================
    # Validation Steps
    # =========================================================

    def ensure_git_repo(self):
        """
        Ensure current directory is a Git repository.
        """
        if not self.gitService.is_git_repo():
            raise ValidationError(
                message="Git repository not found.",
                hint="Run `git init`",
                code="NOT_GIT_REPO"
            )

    def ensure_remote_exists(self, remote: str = "origin"):
        """
        Ensure a required Git remote exists.

        Args:
            remote (str): Remote name to check (default: 'origin').

        Behavior:
            - Uses GitService abstraction
            - Can be skipped via --skip-checks
        """

        if self.skip_checks:
            return

        if not self.gitService.check_remote(remote):
            raise ValidationError(
                message=f"Git remote '{remote}' not found or unreachable.",
                hint=f"Add remote using: git remote add {remote} <url>",
                code="REMOTE_NOT_FOUND",
                context={"remote": remote}
            )

    def ensure_version_file(self):
        """
        Ensure version file exists.
        """

        # print("self.version_file", self.version_file)

        if not self.version_file or not self.version_file.exists():
            raise ValidationError(
                message=f"Version file '{self.version_file}' not found.",
                hint="Run `custy init config`",
                code="VERSION_FILE_NOT_FOUND"
            )

    def ensure_commit_message_file(self):
        """
        Ensure commit message file exists.
        """
        if not self.commit_message_file or not self.commit_message_file.exists():
            raise ValidationError(
                message=f"Commit message file '{self.commit_message_file}' not found.",
                hint="Run `custy init config`",
                code="COMMIT_MSG_FILE_NOT_FOUND"
            )

    def ensure_tag_message_file(self):
        """
        Ensure tag message file exists.
        """
        if not self.tag_message_file or not self.tag_message_file.exists():
            raise ValidationError(
                message=f"Tag message file '{self.tag_message_file}' not found.",
                hint="Run `custy init config`",
                code="TAG_MSG_FILE_NOT_FOUND"
            )

    def ensure_staged_changes(self):
        """
        Ensure there are staged changes before committing.

        Behavior:
        - Checks via GitService
        - Supports auto-stage
        - Supports force commit
        """

        if self.gitService.has_staged_files():
            return

        print("self.auto_stage===========",self.auto_stage)

        # ===== Auto-stage =====
        if self.auto_stage:
            logger.warning("⚠️ No staged changes detected. Auto staging...")
            logger.debug("📦 Auto-running: git add .")

            if self.stage_mode == StageModeChoices.UPDATE:
                self.gitService.auto_stage_update()
            else:
                self.gitService.auto_stage_all()

            if not self.dry_run:
                if not self.gitService.has_staged_files():
                    logger.debug("⚠️ Still no staged files after auto-staging. but proceeding due to --force-commit.")
                    if self.force_commit:
                        logger.warning("⚠️ Proceeding due to --force-commit")
                        return

                    logger.debug("⚠️ [yellow]Warning[/yellow]: Still no staged changes after `git add .`")
                    confirm = input("No staged files. Continue? (y/n): ").strip().lower()
                    if confirm not in ["y", "yes"]:
                        raise ValidationError(
                            message="Aborted due to empty staging.",
                            hint="Stage files using `git add`",
                            code="EMPTY_STAGING_ABORTED"
                        )
                    logger.debug("⚠️ Continuing despite no staged. files.")

            staged_files = self.gitService.list_staged_files()

            if staged_files:
                logger.info("[cyan]📝 Files staged:[/cyan]")
                for f in staged_files:
                    logger.info(f"[green]  - {f}[/green]")
            else:
                logger.debug("⚠️ No files were staged.")
                if self.force_commit:
                    logger.warning("⚠️ Proceeding with empty commit (--force-commit)")
                    return

                raise ValidationError(
                    message="No files staged after auto-stage.",
                    hint="Check your working directory",
                    code="AUTO_STAGE_FAILED"
                )

        elif self.force_commit:
            logger.warning("⚠️ No staged files but continuing (--force-commit)")
            return

        else:
            if self.dry_run:
                logger.warning(
                    "[yellow](dry-run)[/yellow] No staged changes detected. "
                    "Would normally fail with NO_STAGED_CHANGES.\n"
                    "Hint: Use `git add .` or enable --auto-stage."
                )
                return
            raise ValidationError(
                message="No staged changes to commit.",
                hint="Use `git add .`",
                code="NO_STAGED_CHANGES"
            )

    def ensure_commitizen_convention(self):
        """
        Ensure commitizen convention.
        """
        if not self.commitizenHelper.check_commit:
            raise ValidationError(
                message=f"Checking commitizen convention '{self.commit_message_file}' not found.",
                hint="Use cz check --commit-msg-file (path)]",
                code="COMMITIZEN_CONVENTION_FAILED"
            )

    def ensure_commit_message_file_exists(self) -> None:
        """
        Ensure commit message file exists before editing.

        This file is required for:
        - commit message generation
        - user editing phase

        Raises:
            ValidationError: If file is missing.
        """

        if not self.commit_message_file:
            raise ValidationError(
                message="Commit message file path is not provided.",
                hint="Use --message-file or run `custy init templates`.",
                code="COMMIT_MSG_PATH_MISSING",
            )

        if not self.commit_message_file.exists():
            raise ValidationError(
                message=f"Commit message file not found: {self.commit_message_file}",
                hint="Run `custy init templates` to initialize required files.",
                code="COMMIT_MSG_FILE_NOT_FOUND",
                context={"path": str(self.commit_message_file)},
            )

    def ensure_tag_message_file_exists(self) -> None:
        """
        Ensure tag message file exists before editing.

        Raises:
            ValidationError: If file is missing.
        """

        if not self.tag_message_file:
            raise ValidationError(
                message="Tag message file path is not provided.",
                hint="Use --tag-message-file or run `custy init templates`.",
                code="TAG_MSG_PATH_MISSING",
            )

        if not self.tag_message_file.exists():
            raise ValidationError(
                message=f"Tag message file not found: {self.tag_message_file}",
                hint="Run `custy init templates` to initialize required files.",
                code="TAG_MSG_FILE_NOT_FOUND",
                context={"path": str(self.tag_message_file)},
            )
```

I will explain a bit. Basically The User that initialize the custy with `custy init` command, on their own project coding. Let's say the name of the User's project we say 'projA' as alias

And all these configuration can be setting on config.toml by the user if they run `custy init` or they already have that file already

---

- "ensure_git_repo_step"
  Basically like make sure is the repo already exist or initialize on the project 'projA' coding.

Check `ensure_git_repo_step` method on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside that method

- "ensure_remote_exists_step"
  If on config.toml we already defined the remote name, make sure is already exist for example 'origin' or 'upstream' or 'backup' or 'backups' or any name that we already defined on config.toml. Are the defined remote alread defined on the git repo the User's owned on the project 'projA' coding.

Check `ensure_remote_exists_step` method on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside that method

- "ensure_version_file_step"
  Make sure the project 'projA' coding has file like "**version**.py" or something like that. The specific file name or the location see the config.toml configuratiion. You know to indicate the User's project version. For example version 1.2.3

Check `ensure_version_file_step` method on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside that method

- "ensure_staged_changes_step"
  Check if already staged. command like `git add` or `git add .` or `git add [some file]`. Is there already some files that already been staged or not

Check `ensure_staged_changes_step` method on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside that method

- "ensure_commitizen_convention_step"
  This one optionally. If the user use commitizen convention tag strategy instead of using SemVer or PEP440 or other the tag strategy that Custy had offer to use.
  The list of TagStrategy are

```
"CommitizenStrategy", -> Using commitizen convention to generate the tag version
"DateStrategy", -> Using date tag strategy to generate the tag version. For example if the project repo currently has 10 commits and the tag created at "2023.08.10", the tag version will be "2023.08.10". maybe later Custy Update or future release update it will become "v2023.08.10" or "v2023.08.10.1" or "v2023.08.10.2" or "v2023.08.10.3" and so on.
"GitCountStrategy", -> Using Git count tag strategy to generate the tag version. Basically the tag version based on the number of commits on the project repo. For example if the project repo currently has 10 commits, the tag version will be v10. maybe later Custy Update or future release update it will  become 0.0.10. I still consider to use this to count based on number of commit on the repo or number of tag already exist on the repo. I will decide later. But for now I will use the number of commits on the repo to generate the tag version.
"PEP440Strategy", -> Using PEP440 convention to generate the tag version
"SemverStrategy", -> Using SemVer convention to generate the tag version
```

Check `ensure_commitizen_convention` method on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside that method

But so far personally as a Custy Developer, I prefer use SemVer or PEP440 convention to generate the tag version. Or specifically SemVer because Github or Gitlab commonly recognize SemVer convention. But if the User want to use other convention, they can set it on config.toml file.

- "ensure_commit_message_file_step"
- "ensure_commit_message_file_exists_step"

This two part to check if file ".config/custy/templates/commit-message.txt" that already generated using `custy init templates` command.

Check `ensure_commit_message_file` and `ensure_commit_message_file_exists` methods on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside those method

- "ensure_tag_message_file_step"
- "ensure_tag_message_file_exists_step"

This two part to check if file ".config/custy/templates/tag-message.txt" that already generated using `custy init templates` command.

Check `ensure_tag_message_file` and `ensure_tag_message_file_exists` methods on the `workflow_engine.py` file.
See what the message raised and what the hint message showed and whatever inside those method

and the

- "validate"

Run all those validaton steps

---

For references. To see and understand what actually class like `self.gitService`. and the method for example `self.gitService.is_git_repo()`.
To see how they works, please check these 5 files I already attached on This project source's ChatGPT session`. Especially `app/core/git_ops/git/executor.py`
These 5 files related to Custy project itself not docusaurus project documentation. These 5 files are:

```
app/core/git_ops/git/executor.py
app/core/git_ops/git/factory.py
app/core/git_ops/git/protocol.py
app/core/git_ops/git/result.py
app/core/git_ops/git/service.py
```

These commands `custy validate` or `custy run validate` can be run before we want to commit, tag, push. Whether using Custy or just just git tool. But seems before doing some of the Custy workflow

But if you see profiles.py, when we do something like `custy run` or `custy run commit` or `custy run commit tag push` or `custy release` or or `custy full` or whatever they are defined on profiles.py, mostly and seems always run validate at start.
But not if just `custy commit`, `custy tag`, etc the custy command not with run like `custy run`. this command `custy commit` just doing commit. but if we run using `custy run` see the profiles.py. It always need to validate

So basically these commands `custy validate` or `custy run validate` is optionally to run. Because most of the `custy run` workflow run the validate
but not using `custy run` command, then optionally to run `custy validate` or `custy run validate`

Basically It just validate, ensure, make sure, check, verify, confirm, and so on. Before we do the next step like commit, tag, push, release, etc. But if we run using `custy run` command, it will automatically run validate first before doing the next step.

Actually for current step is `custy validate` command. and not yet with `custy run` command. This is just some overviews.

Is there any questions so far?

===================

All good!. No errors.

I will answer your questions

related to these options.

```
--dry-run
--debug
--log-level
```

These are global options

To use it let'se say we want to init
`custy init`
but if we want to debug then
`custy --debug init` and not `custy init --debug`
notice `--debug` parameter is before init command

if we want to validate
`custy validate` or `custy run validate`
but if we want to dry run then
`custy --dry-run validate` and not `custy validate --dry-run`
notice `--dry-run` parameter is before validate command

I send you these 5 files on custy project

This is an entrypoint:

- app\cli\main.py

These are where the global configurations has

- app\cli\commands\main\command.py
- app\cli\commands\main\help.py
- app\cli\commands\main\models.py
- app\cli\commands\main\options.py

Notice

```
def main(
    ctx: typer.Context,
    no_banner: NoBannerOption = None,
    help: HelpOption = None,
    version: VersionOption = None,
    dry_run: DryRunOption = None,
    debug: DebugOption = None,
    log_level: LogLevelOption = None,
):
```

See the help message on `app\cli\main.py` and `app\cli\commands\main\command.py` to understand well

check also `app\cli\commands\validate\command.py` file I send you before. see the help message.

continue

===================

Related to backup command:
I sent you:

From CLI

- `app\cli\commands\backup\command.py`
- `app\cli\commands\backup\models.py`
- `app\cli\commands\backup\options.py`
- `app\cli\commands\backup\resolver.py`

From Core:

- `app\core\backup\backup_manager.py`

Notice backup command has 3 sub command i.e. `commit`, `tag`, `all`

So For the command usage we can do something like for examples:
`custy [optional global arguments] backup commit [optional arguments]`
or `custy [optional global arguments] backup tag [optional arguments]`
or `custy [optional global arguments] backup all [optional arguments]`

===================

Related to backup command:
I sent you:

From CLI

- `app\cli\commands\backup\command.py`
- `app\cli\commands\backup\models.py`
- `app\cli\commands\backup\options.py`
- `app\cli\commands\backup\resolver.py`

From Core:

- `app\core\backup\backup_manager.py`

Notice backup command has 3 sub command i.e. `commit`, `tag`, `all`

So For the command usage we can do something like for examples:
`custy [optional global arguments] backup commit [optional arguments]`
or `custy [optional global arguments] backup tag [optional arguments]`
or `custy [optional global arguments] backup all [optional arguments]`

Also see `app\core\workflow\workflow_engine.py` file from Custy project on this chatgpt project sources.
Especially this part

```
    def backup_commit_message_file(self) -> None:
        """
        Backup commit message file.

        Responsibilities:
        - Backup commit message file (if exists)
        - Store backup in commit backup directory

        Side Effects:
        - creates timestamped backup
        - registers backup file for staging
        """

        if not self.commit_message_file:
            return

        logger.info("[blue]🗂️ Backing up commit message file...[/blue]")

        backup_dir = Path(self.commit_message_backup_dir or CUSTY_BACKUP_COMMIT_DIR)

        self._backup_file(
            source=self.commit_message_file,
            backup_dir=backup_dir,
            label="commit message",
        )


    def backup_tag_message_file(self) -> None:
        """
        Resolve and backup tag message file.

        Responsibilities:
        - Resolve tag message from file (if needed)
        - Backup tag message file (if exists)

        Side Effects:
        - updates self.tag_message (via resolver)
        - creates timestamped backup
        - registers backup file for staging
        """

        logger.info("[blue]🗂️ Processing tag message file...[/blue]")

        # Resolve first (important dependency)
        self._resolve_tag_message_from_file()

        if not self.tag_message_file:
            return

        logger.info("[blue]🗂️ Backing up tag message file...[/blue]")

        backup_dir = Path(self.tag_message_backup_dir or CUSTY_BACKUP_TAG_DIR)

        self._backup_file(
            source=self.tag_message_file,
            backup_dir=CUSTY_BACKUP_TAG_DIR,
            label="tag message",
        )


    def backup_release_files(self) -> None:
        """
        Backup all release-related files.

        This is a high-level orchestration method that delegates
        to more granular backup methods.

        Responsibilities:
        - Backup commit message file
        - Backup tag message file

        Side Effects:
        - creates timestamped backups
        - registers backup files for staging
        """

        logger.info("[blue]🗂️ Backing up release files...[/blue]")

        self.backup_commit_message_file()
        self.backup_tag_message_file()

    def _resolve_tag_message_from_file(self) -> None:
        """
        Resolve tag message content from file or fallback.

        Priority:
            1. tag_message_file content
            2. fallback to tag value

        Side Effects:
            - sets self.tag_message
        """

        if self.tag_message_file and self.tag_message_file.exists():
            self.tag_message = self.tag_message_file.read_text(encoding="utf-8").strip()
            logger.info(f"[dim]📄 Using tag message from {self.tag_message_file}[/dim]")
        else:
            self.tag_message = self.tag
            logger.warning(
                f"[yellow]ℹ️ No tag message file found → using tag as message: {self.tag}[/yellow]"
            )

    def _backup_file(
        self,
        source: Path,
        backup_dir: Path,
        label: str = "file",
    ) -> None:
        """
        Backup a file with timestamp and register it for staging.

        Args:
            source (Path): File to backup
            backup_dir (Path): Destination directory
            label (str): Human-friendly name for logging

        Behavior:
            - creates timestamped backup
            - prunes old backups
            - registers backup files for staging
        """

        if not source.exists():
            logger.debug(f"[dim]Skipping backup (missing {label}): {source}[/dim]")
            return

        # print("backup_dir", backup_dir)

        backup_dir = Path(backup_dir)

        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        backup_dir.mkdir(parents=True, exist_ok=True)

        backup_path = backup_dir / f"{source.stem}_{timestamp}.bak.txt"

        try:
            if self.dry_run:
                logger.info(f"(dry-run) Would backup {label}: {source} → {backup_path}")
                return

            content = source.read_text(encoding="utf-8")
            backup_path.write_text(content, encoding="utf-8")

            logger.info(
                f"🗂️ [dim]{label}[/dim] backed up → [green]{backup_path}[/green]"
            )

            # Register new backup for staging
            self.changes_to_staged.append(backup_path)

            # Prune old backups
            old_files = self.backupManager._prune_old_backups(
                backup_dir=backup_dir,
                stem=source.stem,
            )

            if old_files:
                self.changes_to_staged.extend(old_files)
                logger.debug(f"Pruned old backups: {old_files}")

        except Exception as e:
            logger.error(
                f"[red]⚠️ Failed to backup {label}: {e}[/red]"
            )
```

===================

SO besides
`custy [optional global arguments] backup commit [optional arguments]`
or `custy [optional global arguments] backup tag [optional arguments]`
or `custy [optional global arguments] backup all [optional arguments]`

So we can `custy [optional global arguments] run backup_commit_message`
or `custy [optional global arguments] run backup_tag_message`
or `custy [optional global arguments] run backup_all`

see `app\core\pipeline\profiles.py` file from Custy project on this chatgpt project sources. to check command that we can run with `custy run`

I will explain a bit
why they are many commands to run backups like
without `run` command like `custy [optional global arguments] run backup [sub-command] [optional sub-command arguments]` or with `run` command like `custy [optional global arguments] run [sub-command for 'run' command]`

Basically both do the same way. but with run command, we can add multiple command let's say we want commit and backup the commit message file so we can do command like for example
`custy run commit backup_commit_message`

See we can add multiple command after run command. If we want to commit, tag, push, backup al, then we can do command like for example
`custy run commit tag push backup_all`

or simply `custy release` or `custy full`

see `app\core\pipeline\profiles.py` file from Custy project on this chatgpt project sources.

Please let me know if you can't find `app\core\pipeline\profiles.py` file

===================

---

## (OLD)

This `cleanup_backups` command basically cleanup backups with
`custy [optional global arguments] cleanup backups [optional arguments]` or `custy [optional global arguments] run cleanup_backups`

see `app\core\pipeline\profiles.py` file from Custy project on this chatgpt project sources. to check command that we can run with `custy run`

Notice actually I don't have methods for handle cleanup branches on `app\core\workflow\workflow_engine.py` and on `app\core\pipeline\profiles.py`, specially for sub-command for run like `dev`, `release`, `full` I just have `cleanup_backup_step` on the pipeline workflow and excluded steps like `cleanup_branches_step` or `cleanup_branches` on the `dev`, `release`, `full` sub-command pipeline workflow. Because User's need to do it manually. You know cleanup branches sometimes can break the git branches. maybe it will safe if it is an old branches or we don't need that branches and agree to delete, remove those branches. So for cleanup branches The user's need manually `custy [optional global arguments] cleanup branches [optional arguments]` or `custy [optional global arguments] run cleanup_branches`

So for cleanup branches. need to give warning to User's that want to use the command that some branches. Especially the old branches might deleted, removed or lost. see the files I send you to see how these works

===================

All good!. No errors. continue

So our next steps is for cleanup command, right?

I've updated These file from Custy project and already reupload on this chatgpt project sources.
Double check these files again:

- `config.toml` -> contains settings
- `app\core\git_ops\git\executor.py`
- `app\core\git_ops\git\factory.py`
- `app\core\git_ops\git\protocol.py`
- `app\core\git_ops\git\result.py`
- `app\core\git_ops\git\service.py`
- `app\cli\constants\enums.py`
- `app\utils\parsing.py`
- `app\utils\logging.py`

on file `config.toml` just focus on this part

```
# =========================================================
# CLI - 🧹 CLEANUP - BACKUPS
# =========================================================
#
# Default values used by:
#
#     custy cleanup backup
#
# =========================================================

[tool.custy.cli.cleanup.backup]

# Backup cleanup mode.
#
# Available:
#
#     all
#         Remove both commit and tag backups.
#
#     commit
#         Remove commit backups only.
#
#     tag
#         Remove tag backups only.
#
type = "all"

# Number of most recent backups to keep.
#
# Older backups will be removed.
#
keep = 10

# =========================================================
# CLI - 🌿 CLEANUP - BRANCHES
# =========================================================
#
# Default values used by:
#
#     custy cleanup branch
#
# =========================================================

[tool.custy.cli.cleanup.branch]

# Branch name prefix filter.
#
# Only branches beginning with one of the configured prefixes
# will be considered.
#
# Multiple prefixes are supported.
#
# Examples:
#
# include_prefixes = [
#   "feature/",
#   "release/",
#   "hotfix/",
# ]
#
# Uncomment to enable.
#
# When this option is left empty,
# no branches will be selected for cleanup.
#
# Protected branches are never deleted,
# even if they match one of the configured prefixes.
#
# Default protected branches:
#
#   • main
#   • master
#   • develop
#
# Example:
#
# include_prefixes = [
#   "release/",
#   "ci/",
#   "old/",
# ]
#
# or disable branch cleanup:
#
# include_prefixes = []
#
include_prefixes = []

# Branch merge status filter.
#
# Available values:
#
#     merged
#         Only merged branches.
#
#     unmerged
#         Only unmerged branches.
#
#     all
#         Consider every branch.
#
merge_status = "merged"

# Relative branch age filter.
#
# Only branches whose latest commit is older than the
# specified duration will be considered.
#
# Supported units:
#
#     m   minutes
#     h   hours
#     d   days
#     w   weeks
#     mo  months
#     y   years
#
# Examples:
#
#     30d
#     2w
#     6mo
#     1y
#
# Uncomment to enable.
#
# max_age = "30d"

# Absolute branch date filter.
#
# Only branches whose latest commit occurred before the
# specified date will be considered.
#
# Format:
#
#     YYYY-MM-DD
#
# Example:
#
#     2026-07-01
#
# This option is mutually exclusive with
# "max_age".
#
# Uncomment to enable.
#
# before = "2026-07-01"
```

Files related to command. `custy cleanup`, I send these files:
From CLI:

- `app\cli\commands\cleanup\command.py`
- `app\cli\commands\cleanup\models.py`
- `app\cli\commands\cleanup\options.py`
- `app\cli\commands\cleanup\resolver.py`

Check `app\cli\commands\cleanup\command.py` file and see the help message to understand it well how it works

From Core, cleanup backups related:

- `app\core\cleanup\backups\handle_cleanup_backups.py`

From Core, cleanup branches related:

- `app\core\cleanup\branch\display.py`
- `app\core\cleanup\branch\exceptions.py`
- `app\core\cleanup\branch\handlers.py`
- `app\core\cleanup\branch\models.py`
- `app\core\cleanup\branch\service.py`

From Core, on pipeline part

- `app\core\pipeline\steps\cleanup_backups_step.py`
- `app\core\pipeline\steps\cleanup_branches_step.py`

Additionally

- `app\ui\console.py`
- `app\ui\panels.py`
- `app\ui\tables.py`
  and that's how it showed the result on the CLI

Also I have screenshot that I will send you these screenshots file

- `cleanup-backups-process.png` -> The screenshot when we run the command for `custy cleanup backups` related
- `cleanup-branch-release-before.png` -> The screenshot before the process when we run the command for `custy cleanup branches` related
- `cleanup-branch-release-process.png` -> The screenshot when we run the command for `custy cleanup branches` related
- `cleanup-branch-release-after.png` -> The screenshot after the process when we run the command for `custy cleanup branches` related

---

This `cleanup_backups` command basically cleanup backups with
`custy [optional global arguments] cleanup backups [optional arguments]` or `custy [optional global arguments] run cleanup_backups`

see `app\core\pipeline\profiles.py` file from Custy project on this chatgpt project sources. to check command that we can run with `custy run`

For `custy cleanup backups` command part see ``app\core\cleanup\backups\handle_cleanup_backups.py` to understand how it works. and see `app\core\workflow\workflow_engine.py` file from Custy project on this chatgpt project sources. On this part

```
from app.core.cleanup.backups.handle_cleanup_backups import HandleCleanupBackups
```

and

```
    def cleanup_backups(self) -> None:
        """
        Cleanup old backup files after backup phase.
        """

        cleaner = HandleCleanupBackups(
            target=self.cleanup_backup_type,
            keep=self.backup_retention_count,
        )

        cleaner.cleanup()
```

Check it to understand how it works.

Notice actually I don't have methods for handle cleanup branches on `app\core\workflow\workflow_engine.py` and on `app\core\pipeline\profiles.py`, specially for sub-command for run like `dev`, `release`, `full` I just have `cleanup_backup_step` on the pipeline workflow and excluded steps like `cleanup_branches_step` or `cleanup_branches` on the `dev`, `release`, `full` sub-command pipeline workflow. Because User's need to do it manually. You know cleanup branches sometimes can break the git branches. maybe it will safe if it is an old branches or we don't need that branches and agree to delete, remove those branches. So for cleanup branches The user's need manually `custy [optional global arguments] cleanup branches [optional arguments]` or `custy [optional global arguments] run cleanup_branches`

So for cleanup branches. need to give warning to User's that want to use the command that some branches. Especially the old branches might deleted, removed or lost. see the files I send you to see how these works

But don't worry because User can configure that on config.toml. on this part

```
[tool.custy.cli.cleanup.branch]

# Branch name prefix filter.
#
# Only branches beginning with one of the configured prefixes
# will be considered.
#
# Multiple prefixes are supported.
#
# Examples:
#
# include_prefixes = [
#   "feature/",
#   "release/",
#   "hotfix/",
# ]
#
# Uncomment to enable.
#
# When this option is left empty,
# no branches will be selected for cleanup.
#
# Protected branches are never deleted,
# even if they match one of the configured prefixes.
#
# Default protected branches:
#
#   • main
#   • master
#   • develop
#
# Example:
#
# include_prefixes = [
#   "release/",
#   "ci/",
#   "old/",
# ]
#
# or disable branch cleanup:
#
# include_prefixes = []
#
include_prefixes = []
```

if include_prefixes is empty list then no branch will remove. SO user's need to specify the branch. We can filters by time on this part. basically most of the configuration on `config.toml` related to command `custy cleanup branch` has filters configurations.

Also notice this part

```
# Default protected branches:
#
#   • main
#   • master
#   • develop
```

These protected branches will not be deleted if using Custy. If the user's really want to delete those branches. They can't use Custy, instead using git tools directly.

Besides Custy project I will send again current Docusaurus project structure inside `src/`

```
├── src
│   ├── components
│   │   ├── docs
│   │   │   ├── architecture
│   │   │   │   ├── ArchitectureCard
│   │   │   │   ├── DependencyCard
│   │   │   │   ├── LayerCard
│   │   │   │   ├── PatternCard
│   │   │   │   ├── PatternTable
│   │   │   │   └── index.ts
│   │   │   ├── command
│   │   │   │   ├── CommandChecklist
│   │   │   │   ├── CommandExample
│   │   │   │   ├── CommandHero
│   │   │   │   ├── CommandLifecycle
│   │   │   │   ├── CommandMetadata
│   │   │   │   ├── CommandOption
│   │   │   │   ├── CommandOutput
│   │   │   │   ├── CommandSyntax
│   │   │   │   ├── RelatedCommands
│   │   │   │   └── index.ts
│   │   │   ├── common
│   │   │   │   ├── Badge
│   │   │   │   ├── Card
│   │   │   │   ├── Divider
│   │   │   │   ├── Grid
│   │   │   │   ├── Heading
│   │   │   │   ├── Icon
│   │   │   │   ├── InfoBox
│   │   │   │   ├── Section
│   │   │   │   ├── Tag
│   │   │   │   └── index.ts
│   │   │   ├── feature
│   │   │   │   ├── FeatureCard
│   │   │   │   ├── FeatureGrid
│   │   │   │   ├── FeatureList
│   │   │   │   └── index.ts
│   │   │   ├── hooks
│   │   │   │   ├── index.ts
│   │   │   │   ├── useClipboard.ts
│   │   │   │   ├── useMounted.ts
│   │   │   │   └── useThemeMode.ts
│   │   │   ├── mermaid
│   │   │   │   ├── MermaidCard
│   │   │   │   ├── MermaidLegend
│   │   │   │   ├── MermaidTitle
│   │   │   │   └── index.ts
│   │   │   ├── requirement
│   │   │   │   ├── RequirementCard
│   │   │   │   ├── RequirementGrid
│   │   │   │   └── index.ts
│   │   │   ├── screenshot
│   │   │   │   ├── ImageCompare
│   │   │   │   ├── Screenshot
│   │   │   │   ├── ScreenshotGrid
│   │   │   │   └── index.ts
│   │   │   ├── shared
│   │   │   │   └── icons
│   │   │   ├── styles
│   │   │   │   ├── animation.css
│   │   │   │   ├── components.css
│   │   │   │   ├── index.css
│   │   │   │   ├── layout.css
│   │   │   │   ├── typography.css
│   │   │   │   ├── utilities.css
│   │   │   │   └── variables.css
│   │   │   ├── table
│   │   │   │   ├── KeyValueTable
│   │   │   │   ├── PropertyTable
│   │   │   │   └── index.ts
│   │   │   ├── terminal
│   │   │   │   ├── CopyButton
│   │   │   │   ├── Terminal
│   │   │   │   ├── TerminalBody
│   │   │   │   ├── TerminalHeader
│   │   │   │   └── index.ts
│   │   │   ├── types
│   │   │   │   ├── common.ts
│   │   │   │   ├── component.ts
│   │   │   │   ├── index.ts
│   │   │   │   ├── layout.ts
│   │   │   │   ├── status.ts
│   │   │   │   └── theme.ts
│   │   │   ├── utils
│   │   │   │   ├── array.ts
│   │   │   │   ├── copy.ts
│   │   │   │   ├── format.ts
│   │   │   │   ├── index.ts
│   │   │   │   ├── object.ts
│   │   │   │   ├── slug.ts
│   │   │   │   ├── status.ts
│   │   │   │   ├── text.ts
│   │   │   │   └── validation.ts
│   │   │   ├── workflow
│   │   │   │   ├── WorkflowDiagram
│   │   │   │   ├── WorkflowLegend
│   │   │   │   ├── WorkflowStage
│   │   │   │   ├── WorkflowStep
│   │   │   │   ├── WorkflowTimeline
│   │   │   │   └── index.ts
│   │   │   ├── index.ts
│   │   │   └── README.md
│   │   └── homepage
│   │       ├── About.module.css
│   │       ├── About.tsx
│   │       ├── about.txt
│   │       ├── Audiences.module.css
│   │       ├── Audiences.tsx
│   │       ├── DocumentationModel.module.css
│   │       ├── DocumentationModel.tsx
│   │       ├── DocumentationOverview.module.css
│   │       ├── DocumentationOverview.tsx
│   │       ├── Hero.module.css
│   │       ├── Hero.tsx
│   │       ├── ProjectGrid.module.css
│   │       ├── ProjectGrid.tsx
│   │       ├── QuickLinks.module.css
│   │       └── QuickLinks.tsx
│   ├── css
│   │   ├── custom.css
│   │   ├── docs.css
│   │   └── docs.old.css
│   ├── data
│   │   └── projects.ts
│   ├── logs
│   ├── pages
│   │   ├── logs
│   │   ├── index.module.css
│   │   ├── index.tsx
│   │   └── markdown-page.mdx
│   └── __version__.py
```

But not all files I included there.

So far. Is there any questions or needed some files from my end?

===================

import enum

Related to command `custy version`

Currently it just have 1 sub-command i.e. update. Maybe inn the future Custy project update or release we will add more.

For now the subcommand it just `update`. So it become `custy version update`

Files related to command. `custy version`, I send these files:
From CLI:

- `app\cli\commands\version\command.py`
- `app\cli\commands\version\models.py`
- `app\cli\commands\version\options.py`
- `app\cli\commands\version\resolver.py`

Check `app\cli\commands\version\command.py` file and see the help message to understand it well how it works

From core:

- `app\core\files\update_files.py` -> is where `update_version_universal` command called on `app\core\workflow\workflow_engine.py` file from Custy project.
- `app\core\git_ops\helper\project_detector.py` -> is where `detect_project_strategy` command called on `app\core\workflow\workflow_engine.py` file from Custy project.
- `app\core\git_ops\versioning\version_bridge.py` -> is where `VersionBridge` class called. also where `VersionBridge.semver_to_pep440()` method called on `app\core\workflow\workflow_engine.py` file from Custy project.
- `app\core\git_ops\helper\commitizen.py` -> is where `CommitizenHelper` called. Also where `commitizenHelper.update_cz_toml_version()` method called on `app\core\workflow\workflow_engine.py` file from Custy project
- `app\core\git_ops\tag_strategy\base.py`
- `app\core\git_ops\tag_strategy\commitizen_strategy.py`
- `app\core\git_ops\tag_strategy\date_strategy.py`
- `app\core\git_ops\tag_strategy\git_count_strategy.py`
- `app\core\git_ops\tag_strategy\pep440_strategy.py`
- `app\core\git_ops\tag_strategy\semver_strategy.py`

see `app\cli\constants\enums.py` file from Custy project on this chatgpt project source, to see `StrategyChoices` enum class defined

If run using `run` command. we can do `custy run apply_version`

Please check `app\core\pipeline\profiles.py` file from Custy project on this chatgpt project sources.

On this part. You can see the steps

```
# Apply version update
    "apply_version": [
        {"name": "validate-sub-repo"},
        {"name": "ensure_version_file_step"},
        {"name": "prepare_version_step"},
        {"name": "prepare_tag_message_step"},
        {"name": "apply_version_step"},
    ],
```

and Please check `app\core\workflow\workflow_engine.py` file from Custy project on this chatgpt project sources.

Related parts are

```
    def __init__(
        self,
        config: WorkflowConfig
    ) -> None:
        # =========================================================
        # 📦 Configuration (immutable input)
        # =========================================================
        self.config = config

        # ===== Inputs =====
        self.commit_message_input = config.commit_message_input
        self.commit_message_file = config.commit_message_file
        self.tag_input = config.tag_input
        self.tag_message_input = config.tag_message_input
        self.tag_message_file = config.tag_message_file
        self.strategy_input = config.strategy
        self.bump_level = config.bump_level
        self.pre_release = config.pre_release
        self.post_release = config.post_release
        self.dev_release = config.dev_release
        self.meta = config.meta
        self.epoch = config.epoch
        self.force_tag = config.force_tag
        self.version_file = config.version_file
        self.auto_stage = config.auto_stage
        self.stage_mode = config.stage_mode
        self.force_changelog = config.force_changelog
        self.force_commit = config.force_commit
        self.sync_backup = config.sync_backup
        self.skip_checks = config.skip_checks

        self.main_remotes = config.main_remotes
        self.backup_remotes = config.backup_remotes

        self.commit_message_backup_dir = config.commit_message_backup_dir
        self.tag_message_backup_dir = config.tag_message_backup_dir
        self.cleanup_backup_type = config.cleanup_backup_type
        self.backup_retention_count = config.backup_retention_count

        self.dry_run = config.dry_run
        self.no_debug = config.no_debug
        self.log_level = config.log_level

        # ===== Runtime =====
        self.tag: str = ""
        self.tag_message: str = ""
        self.changes_to_staged: List[str] = []

        # =========================================================
        # 🔧 Core Services
        # =========================================================
        self.gitService: GitService = create_git_service(dry_run=self.dry_run)
        self.commitizenHelper = CommitizenHelper(dry_run=self.dry_run)
        self.changelogGenerator = ChangelogGenerator(dry_run=self.dry_run)
        self.backupManager = BackupManager(keep=self.backup_retention_count)
        self.branchWorkflowManager = BranchWorkflowManager(
            no_debug=self.no_debug,
            sync_backup=self.sync_backup,
            dry_run=self.dry_run,
        )

        # Silent mode
        if self.no_debug:
            self._set_silent_mode()
            logger.debug("🐞 No Debug")
```

and

```
    # =========================================================
    # Validation Steps
    # =========================================================

    def ensure_git_repo(self):
        """
        Ensure current directory is a Git repository.
        """
        if not self.gitService.is_git_repo():
            raise ValidationError(
                message="Git repository not found.",
                hint="Run `git init`",
                code="NOT_GIT_REPO"

    def ensure_version_file(self):
        """
        Ensure version file exists.
        """

        # print("self.version_file", self.version_file)

        if not self.version_file or not self.version_file.exists():
            raise ValidationError(
                message=f"Version file '{self.version_file}' not found.",
                hint="Run `custy init config`",
                code="VERSION_FILE_NOT_FOUND"

    def prepare_version_tag(self) -> None:
        """
        Determine the next version tag based on input or strategy.

        Priority:
        1. Explicit tag input (--tag)
        2. Strategy-based generation

        Side Effects:
            - sets self.tag
        """

        if self.tag_input:
            self.tag = self.tag_input

        elif self.strategy_input == StrategyChoices.SEMVER:
            self.tag = SemverStrategy(
                bump=self.bump_level,
                pre_release=self.pre_release,
                build_meta=self.meta,
                no_debug=self.no_debug,
            ).get_next_tag()

        elif self.strategy_input == StrategyChoices.PEP440:
            self.tag = PEP440Strategy(
                bump=self.bump_level,
                pre_release=self.pre_release,
                post_release=self.post_release,
                dev_release=self.dev_release,
                local=self.meta,
                epoch=self.epoch,
                no_debug=self.no_debug,
            ).get_next_tag()

        elif self.is_commitizen_auto():
            self.tag = CommitizenStrategy(self.pre_release).get_next_tag()

        elif self.strategy_input == StrategyChoices.DATE:
            self.tag = DateStrategy().get_next_tag()

        elif self.strategy_input == StrategyChoices.GIT_COUNT:
            self.tag = GitCountStrategy().get_next_tag()

        else:
            raise ValidationError(
                message="Invalid versioning configuration.",
                hint="Provide --tag or use --strategy with --bump.",
                code="INVALID_VERSION_INPUT",
            )

        logger.info(f"🔖 Next version: {self.tag}")

    def prepare_tag_message(self) -> None:
        """
        Resolve or initialize tag message.

        Priority:
        1. CLI input (--tag-msg)
        2. File (--tag-msg-file)
        3. Default template

        Side Effects:
            - sets self.tag_message
            - may create tag message file
        """

        if self.tag_message_input:
            self.tag_message = self.tag_message_input
            logger.info("✅ Using tag message from CLI.")
            return

        tag_msg_path = Path(self.tag_message_file or CUSTY_TAG_MESSAGE_TEMPLATE)

        if not tag_msg_path.exists():
            default_content = (
                f"Release {self.tag}\n\n# Write additional tag notes below\n"
            )

            tag_msg_path.parent.mkdir(parents=True, exist_ok=True)
            tag_msg_path.write_text(default_content, encoding="utf-8")

            logger.info(f"📝 Created tag message template: {tag_msg_path}")

        self.tag_message_file = tag_msg_path
```

and

```
    def apply_version_updates(self) -> None:
        """
        Apply all version-related updates before staging and commit.

        This includes:
        - updating Python version file (__version__.py)
        - updating project config files (pyproject.toml, package.json)

        This step prepares the repository state for commit.

        Logic:
            1. Detect project type (python / semver / others)
            2. If Python project:
                - Force PEP440 version format (conversion if needed)
                - Update python version file
                - Update cz.toml
                - Update ONLY python-related files
            3. If non-python (e.g. Node / SemVer):
                - Skip python-specific updates
                - Update ONLY relevant project files

        Notes:
            - This method overrides strategy ONLY for version update phase
            - Does NOT affect tagging strategy
        """

        logger.info("🔧 Applying version updates...")

        # =========================================================
        # Detect project type
        # =========================================================
        project_strategy = detect_project_strategy(
            cli_value=None,
            no_debug=self.no_debug,
        )

        logger.debug(f"Detected project strategy: {project_strategy}")

        # =========================================================
        # CASE 1: Python Project (PEP440 enforced here)
        # =========================================================
        if project_strategy == StrategyChoices.PEP440:
            logger.info("🐍 Python project detected → using PEP440 for version updates")

            version_to_use = self.tag

            # Convert if current tag is SemVer-like
            if "-" in self.tag:
                converted = VersionBridge.semver_to_pep440(self.tag)
                if converted:
                    logger.debug(f"Converted SemVer → PEP440: {self.tag} → {converted}")
                    version_to_use = converted

            # ---- Python version file ----
            self.update_python_version_file(version_override=version_to_use)

            # ---- cz.toml ----
            self.commitizenHelper.update_cz_toml_version(
                new_version=version_to_use
            )

            # ---- Project files (ONLY python) ----
            self.update_project_versions(
                version_override=version_to_use,
                project_type="python",
            )

        # =========================================================
        # CASE 2: Non-Python (SemVer / JS / etc.)
        # =========================================================
        else:
            logger.info("🌐 Non-Python project → skipping Python-specific updates")

            # Only update project files (Node / etc.)
            self.update_project_versions(
                version_override=self.tag,
                project_type="node",
            )

        # # If project type == python
        # self.update_python_version_file()
        # self.commitizenHelper.update_cz_toml_version(new_version=self.tag)

        # self.update_project_versions()
        # # self.generate_changelog_if_needed()

    def update_python_version_file(self, version_override: Optional[str] = None) -> None:
        """
        Update Python version file (e.g. __version__.py).

        Behavior:
            - writes version string
            - respects dry-run
            - validates file existence

        Args:
            version_override (str | None): override version (used for PEP440 conversion)
        """

        if not self.version_file:
            logger.debug("No version file configured → skipping.")
            return

        if not self.version_file.exists():
            raise ValidationError(
                message=f"Version file not found: {self.version_file}",
                hint="Ensure version file exists or configure it properly.",
                code="VERSION_FILE_NOT_FOUND",
            )

        version = version_override or self.tag

        content = f'# {self.version_file.name}\n\n__version__ = "{version}"\n'

        if self.dry_run:
            logger.info(f"(dry-run) Would update {self.version_file} → {version}")
            return

        self.version_file.write_text(content, encoding="utf-8")

        logger.info(
            f"✅ [dim]{self.version_file}[/dim] [green]updated[/green] → {version}"
        )

    def update_project_versions(
        self,
        version_override: Optional[str] = None,
        project_type: Optional[str] = None,
        ) -> None:
        """
        Update project-level version files.

        Supported:
            - pyproject.toml
            - package.json (Node)
            - other supported formats

        Args:
            version_override (str | None): version to apply
            project_type (str): 'python' | 'node' | None

        Behavior:
            - python → update pyproject.toml only
            - node → update package.json only
            - None → fallback to universal

        Uses:
            update_version_universal()
        """

        version = version_override or self.tag

        if self.dry_run:
            logger.info("(dry-run) Would update project version files.")
            return

        updated = update_version_universal(
            root=".",
            new_version=version,
            project_type=project_type,
        )

        if not updated:
            logger.debug("No project version files updated.")
```

on `update_python_version_file()` method, see

```
self.version_file.write_text(content, encoding="utf-8")
```

The file for variable `self.version_file` is basically `version_file: Optional[Path] = None` where the default value is "app/**version**.py" if you see on file `app\cli\commands\version\resolver.py`. So basically `update_python_version_file()` method update "app/**version**.py" specified by the user. It can also configured on config.toml. Basically update the version to latest or current version based on user's needed

Also if you see config.toml. Focus on this part

```

#     custy run full
#
# Examples:
#
#     app/__version__.py
#     src/package/__version__.py
#
version_file = "app/__version__.py"
```

and

```
# =========================================================
# CLI - 🏷️ VERSIONING
# =========================================================
#
# Default values used by:
#
#     custy commit
#     custy tag
#     custy release
#
# =========================================================

[tool.custy.cli.versioning]

# Versioning strategy.
#
# Available:
#
#     semver
#     pep440
#     commitizen
#     date
#     gitcount
#
# Remove or comment this value to allow
# automatic project strategy detection.
#
strategy = "semver"

# Default bump strategy.
#
# Available:
#
#     auto
#     patch
#     minor
#     major
#
bump = "auto"
```

Related to tag strategy choices based on `app\cli\constants\enums.py` on `StrategyChoices` enum class

```
class StrategyChoices(str, Enum):
    SEMVER = "semver"
    PEP440 = "pep440"
    DATE = "date"
    GIT_COUNT = "gitcount"
    COMMITIZEN = "commitizen"
    NONE = None
```

check these files

```
- `app\core\git_ops\tag_strategy\base.py`
- `app\core\git_ops\tag_strategy\commitizen_strategy.py`
- `app\core\git_ops\tag_strategy\date_strategy.py`
- `app\core\git_ops\tag_strategy\git_count_strategy.py`
- `app\core\git_ops\tag_strategy\pep440_strategy.py`
- `app\core\git_ops\tag_strategy\semver_strategy.py`
```

So far. Is there any questions or needed some files from my end?

===================

Related to command like `custy commit`, `custy tag`, `custy commit tag`, `custy push`, `custy commit push`, `custy commit tag`, `custy dev`, `custy release`, `custy full`. etc
I sent these files. I can't sent all the files because it is too many. But I explain a bit.

Start by command like `custy version update` and `custy changelog generate`

Related to update files. for example when run `custy version update`
I sent you:

- `app\core\files\update_files.py`

Related to generate changelog like CHANGELOG.md file. for example when run `custy changelog generate`

- The `app\core\git_ops\versioning\builder\release_builder.py` basically script to generate changelog. For example CHANGELOG.md. The related files like `app\core\git_ops\versioning\models\release_info.py` and `app\core\git_ops\versioning\models\version_type.py`. And also `changelog.j2` I send is the templates used
- The `app\core\git_ops\versioning\version_bridge.py`, basically a bridge among different tag format startegy
- `app\core\git_ops\commit\validator.py` Is where it validate the commit message. the commit message should follow specific formats defined.

===========

import enum

Related to command `custy changelog`

Currently it just have 1 sub-command i.e. `generate`. Maybe inn the future Custy project update or release we will add more.

For now the subcommand it just `update`. So it become `custy changelog generate`

Files related to command. `custy changelog`, I send these files:
From CLI:

- `app\cli\commands\changelog\command.py`
- `app\cli\commands\changelog\models.py`
- `app\cli\commands\changelog\options.py`
- `app\cli\commands\changelog\resolver.py`

Check `app\cli\commands\changelog\command.py` file and see the help message to understand it well how it works

From core:

- `app\core\pipeline\steps\generate_changelog_step.py`
- `app\core\changelog\changelog_cleaner.py`
- `app\core\changelog\changelog_generator.py`
- `app\core\changelog\message_aggregator.py`
- `app\core\changelog\message_providers.py`
- `app\core\changelog\pipeline.py`
- `app\config\config_loader.py`

Related to git Operations, There are 5 files. Check them from Custy project on this chatgpt project sources.

- `app\core\git_ops\git\executor.py`
- `app\core\git_ops\git\factory.py`
- `app\core\git_ops\git\protocol.py`
- `app\core\git_ops\git\result.py`
- `app\core\git_ops\git\service.py`

see `app\core\workflow\workflow_engine.py`

If run using `run` command. we can do `custy run changelog`

Please check `app\core\pipeline\profiles.py` file from Custy project on this chatgpt project sources.

On this part. You can see the steps

```
# Changelog
    "changelog": [
        {"name": "generate_changelog_step"}
    ],
```

and Please check `app\core\workflow\workflow_engine.py` file from Custy project on this chatgpt project sources.

Related parts are

```
    def __init__(
        self,
        config: WorkflowConfig
    ) -> None:
        # =========================================================
        # 📦 Configuration (immutable input)
        # =========================================================
        self.config = config

        # ===== Inputs =====
        self.commit_message_input = config.commit_message_input
        self.commit_message_file = config.commit_message_file
        self.tag_input = config.tag_input
        self.tag_message_input = config.tag_message_input
        self.tag_message_file = config.tag_message_file
        self.strategy_input = config.strategy
        self.bump_level = config.bump_level
        self.pre_release = config.pre_release
        self.post_release = config.post_release
        self.dev_release = config.dev_release
        self.meta = config.meta
        self.epoch = config.epoch
        self.force_tag = config.force_tag
        self.version_file = config.version_file
        self.auto_stage = config.auto_stage
        self.stage_mode = config.stage_mode
        self.force_changelog = config.force_changelog
        self.force_commit = config.force_commit
        self.sync_backup = config.sync_backup
        self.skip_checks = config.skip_checks

        self.main_remotes = config.main_remotes
        self.backup_remotes = config.backup_remotes

        self.commit_message_backup_dir = config.commit_message_backup_dir
        self.tag_message_backup_dir = config.tag_message_backup_dir
        self.cleanup_backup_type = config.cleanup_backup_type
        self.backup_retention_count = config.backup_retention_count

        self.dry_run = config.dry_run
        self.no_debug = config.no_debug
        self.log_level = config.log_level

        # ===== Runtime =====
        self.tag: str = ""
        self.tag_message: str = ""
        self.changes_to_staged: List[str] = []

        # =========================================================
        # 🔧 Core Services
        # =========================================================
        self.gitService: GitService = create_git_service(dry_run=self.dry_run)
        self.commitizenHelper = CommitizenHelper(dry_run=self.dry_run)
        self.changelogGenerator = ChangelogGenerator(dry_run=self.dry_run)
        self.backupManager = BackupManager(keep=self.backup_retention_count)
        self.branchWorkflowManager = BranchWorkflowManager(
            no_debug=self.no_debug,
            sync_backup=self.sync_backup,
            dry_run=self.dry_run,
        )

        # Silent mode
        if self.no_debug:
            self._set_silent_mode()
            logger.debug("🐞 No Debug")
```

and

```
def generate_changelog_if_needed(self) -> None:
        """
        Generate changelog file if version is eligible.

        Rules:
            - Only for final releases (unless forced)
            - Uses ChangelogGenerator

        Side Effects:
            - writes CHANGELOG.md
        """

        if not maybe_assert_is_final(
            self.tag,
            context="changelog",
            force=self.force_changelog,
        ):
            logger.debug("Skipping changelog generation (not final version).")
            return

        logger.info("[blue]📜 Generating changelog...[/blue]")

        if self.dry_run:
            logger.info("(dry-run) Would generate CHANGELOG.md")
            return

        rendered = self.changelogGenerator.generate()

        self.changelogGenerator.write_to_files(
            content=rendered,
            path=CHANGELOG_PATH,
        )

        logger.info(f"✅ {CHANGELOG_PATH} updated.")
```

Also if you see config.toml. Focus on this part

```
# =========================================================
# CLI - 📄 TEMPLATE FILES
# =========================================================
#
# Template files used by Custy when generating
# commit messages, tag messages, and changelogs.
#
# These values are used as defaults when
# corresponding CLI options are not supplied.
#
# Used by:
#
#     custy commit
#     custy tag
#     custy changelog
#     custy run
#
# =========================================================

[tool.custy.cli.templates.file]

# Changelog template file.
#
# Supported:
#
#     Jinja2 (.j2)
#
# Examples:
#
#     .config/custy/templates/changelog/changelog.j2
#     tools/custy/templates/changelog/changelog.j2
#     templates/changelog.j2
#
changelog = ".config/custy/templates/changelog/changelog.j2"

# Commit message template.
#
# Used when generating commit messages
# for automated commit workflows.
#
# Examples:
#
#     .config/custy/templates/commit-message.txt
#     tools/custy/templates/commit-message.txt
#     templates/commit-message.txt
#
commit_message = ".config/custy/templates/commit-message.txt"

# Tag message template.
#
# Used when generating annotated Git tags.
#
# Examples:
#
#     .config/custy/templates/tag-message.txt
#     tools/custy/templates/tag-message.txt
#     templates/tag-message.txt
#
tag_message = ".config/custy/templates/tag-message.txt"
```

and

```
# =========================================================
# CLI - 📝 CHANGELOG
# =========================================================
#
# Default values used by:
#
#     custy changelog
#
# =========================================================

[tool.custy.cli.changelog]

# Force changelog generation even when
# no new releases or commits are detected.
#
force_generation = false
```

===================
