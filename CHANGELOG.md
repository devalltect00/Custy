# 🧾 Changelog

All notable changes to this project are documented in this file.

This changelog is automatically generated by Custy from structured commit messages.

## Unreleased

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v2.0.0...HEAD

**Release**
Unreleased

**Summary**

Add the private GitLab PyPI distribution path for Custy and harden the release
boundary before the v2.1.0 stable promotion.

### ⚙️ CI/CD

- Restore active build, cleanup-backup, test, and helper paths to the Git and
- Remove tracked `.pyc` artifacts from the repository index while preserving
- Keep Python 3.14 CI validation aligned with the active source and test trees.
- Make CLI help assertions deterministic across ANSI-capable runners and
- Preserve verbose pytest output in CI for actionable failure diagnostics.
- Confirm the GitHub Actions and GitLab CI validation pipelines complete

### 🚀 Releases

- Stop the GitHub production Docker workflow from running for ordinary pushes
- Build automatically only when a supported `v*` release tag is pushed.
- Require manual production rebuilds to select an existing release tag instead
- Require an annotated, non-empty Git tag message before image publication.
- Verify the normalized package build version matches the selected release tag.
- Publish the exact stable or prerelease tag and update `latest` only for a
- Remove the redundant human-readable commit-SHA image tag while retaining
- Bring GitLab production publishing under the same annotated-tag contract,
- Replace the placeholder GitLab Release description with the complete

#### Workflow

- Preserve non-empty commit and tag message files as reviewed project-owned
- Generate fallback messages only when the configured file is missing or
- Keep generated fallback files newline-terminated for formatting-hook

### Validation

- Validate workflow structure, diff whitespace, Ruff, Black, pre-commit, and
- Preserve Buildx provenance; untagged manifests referenced by a tagged OCI
- Leave existing registry packages untouched and apply the tag-only policy only

#### Distribution

- Passed the full Custy suite: 1,341 tests.
- Passed the focused Python-package workflow regression test.
- Passed Ruff, Black, GitLab YAML parsing, and repository diff checks.

### Credential Policy And Storage

#### Credentials

- Add strict `native` and `auto` credential modes under
- Keep `container_only = true` as the recommended boundary so local Git
- Add ordered `file` and `environment` sources for GitHub and GitLab tokens.
- Store only provider policy, usernames, external filenames, and environment
- Resolve external files from platform-specific user data directories or
- Reject token files inside the target project, symbolic links, non-regular
- Write token files atomically with restrictive permissions and require an

### Native First Git Integration

#### Credentials

- Preserve normal Git credential helpers and SSH authentication as the first
- Add `git-credential-custy` as a per-process helper only for an enabled,
- Avoid system, global, and repository Git configuration changes by injecting
- Leave SSH remotes and unsupported or lookalike hosts entirely on native Git
- Allow a missing external file to fall through to the configured environment
- Preserve interactive username/PAT prompts when policy allows them, give Git
- Disable Rich rendering entirely for hidden direct-command pipelines so
- Explain before a manual HTTPS prompt that Git's `Password` field expects a
- Apply the same policy to branch pushes, tag pushes, and read-only remote

### Configure Credentials Cli

#### Credentials

- Add the guided `custy configure credentials` workflow.
- Add focused `set`, `status`, `test`, and `remove` subcommands for GitHub and
- Prompt for file-backed tokens with hidden input and confirmation; deliberately
- Reject hidden token entry when no controllable TTY is attached and direct
- Show source availability and external paths without printing token values.
- Test access with read-only `git ls-remote` and provide an explicit file-delete
- Keep dry-run secret-free and network-free: it does not read token material,

### Docker Compose And Remote Image Make Helpers

#### Credentials

- Add `d-credentials-*`, `c-credentials-*`, and
- Keep `CUSTY_CREDENTIALS_MOUNT=false` as the safe runtime default so existing
- Allow an explicit `CUSTY_CREDENTIALS_MOUNT=true` on Docker, Compose, and
- Use a writable mount only for credential `set`; keep status, test, and
- Add platform-aware host-directory defaults and explicit host, container, and

### Configuration Diagnostics And Compatibility

#### Credentials

- Document the native/auto and container-only behavior matrix directly in the
- Add GitHub defaults (`x-access-token`, `CUSTY_GITHUB_TOKEN`) and GitLab
- Update push authentication hints to direct users to SSH, an interactive Git
- Preserve the original executor call shape in native mode and omit empty
- Register the helper as a packaged console entry point without logging or

### 📚 Documentation

- Document automatic project-source and version-target detection in the canonical Custy guide.
- Explain initialization and versioning behavior for Python, Node.js, mixed, and generic repositories.
- Add Docker guidance for repositories without an `app/` directory.
- Add migration and troubleshooting guidance for older explicit path configuration.

#### Credentials

- Add focused settings, storage, provider-matching, precedence, malformed-file,
- Verify hidden direct pipelines do not claim the terminal and direct Git
- Verify the targeted credential and Git compatibility suites before the full
- Validate the complete 1,340-test suite with 83% overall coverage, plus Ruff,
- Update the canonical English and Indonesian documentation for configuration,
- Pass TypeScript validation and production builds for both documentation

#### Editor

- Document candidate precedence, supported identifiers, aliases, fallback controls, and structured failures.
- Explain `VISUAL` and `EDITOR` overrides, the Windows `code.cmd` behavior, and the boundary between host and container editors.
- Document interactive Docker requirements, bundled editors, key bindings, and the recommended `-it` invocation.
- Add direct Docker build guidance for supplying an explicit Custy package version.
- Add recovery guidance for failures at `EditFilesStep`, including the boundary between generated artifacts and later Git mutations.
- Explain expected progress suspension during terminal editing and how to identify an outdated executable or cached image when stale rows remain.
- Keep the English and Indonesian Custy documentation behaviorally aligned.

#### Workflow

- Add `commitizen` and `hooks` installation extras and include both in the
- Document the validation-provider and Git-hook behavior matrices in generated
- Add matching English and Indonesian guidance for configuration, commit,
- Add provider resolution, hook-policy, direct pre-commit, no-hook project,

### Commit Validation Providers

#### Workflow

- Add `auto`, `custy`, `commitizen`, and `git` providers under
- Make `auto` select Commitizen only when project configuration is present and
- Fall back to Custy's built-in Conventional Commit validation when optional
- Let projects require the external tool with `require_tool = true`.
- Keep `--check-cz` as a backward-compatible strict Commitizen override.
- Keep version-strategy selection independent from commit-message validation.

### Git And Pre Commit Execution

#### Workflow

- Preserve Git stdout, stderr, operation name, and exit code when a commit
- Classify hook-originated failures as `GIT_HOOK_FAILED` and show the hook's
- Keep ordinary Git commit failures distinct as `GIT_COMMIT_FAILED`.
- Confirm that `git commit -F <message-file>` remains the correct file-based
- Add `auto`, `native`, and `pre_commit` execution policies under
- Keep repositories without installed hooks on Git's normal native path; a
- Detect Windows-generated pre-commit wrappers that cannot execute inside a
- Use `git commit --no-verify` only after every bypassed pre-commit-managed
- Report policy and direct-hook failures with actionable error codes before a
- Preserve or add the final newline in generated Python version modules so

### Docker Push Workflow And Diagnostics

#### Workflow

- Resolve standalone `custy push` and `custy run push` through repository
- Give push and workflow-initialization steps exclusive terminal ownership so
- Explain at runtime that a Linux container uses credentials available inside
- Preserve Git push stdout, stderr, operation, exit code, remote, and tag
- Distinguish likely authentication failures and provide recovery guidance for
- Keep authentication external to Custy; this checkpoint does not add token

### 🧪 Tests

- Add focused detector coverage for Python, Node.js, mixed, PHP, and generic layouts.
- Add cross-project version-update tests for Python modules, `pyproject.toml`, `package.json`, lockfiles, mixed repositories, and tag-only repositories.
- Add protected-entry-point, structured-error, no-subcommand, and cross-project startup regression tests.
- Update initialization, resolver, validation, and workflow tests for optional automatic version targets.
- Align Ruff and Black configuration with the active source tree while excluding protected historical and temporary reference files.
- Apply import, formatting, typing, and lint cleanup across active application and test code.
- Validate the refreshed pre-commit configuration before release.
- Validate the rebuilt `custy-prod:latest` image from the Devalltect documentation repository.

#### Editor

- Add unit coverage for configuration parsing, aliases, platform and container precedence, fallback controls, and invalid values.
- Add unit coverage for resolved executable launching, TTY requirements, dry-run simulation, missing files, and launch failures.
- Add regression coverage for the Docker editor packages, settings files, and expected key bindings.
- Add unit coverage for exact tags, release candidates, development versions, post releases, Git commit distance, and missing Git metadata.
- Add workflow integration coverage proving edit steps delegate correctly in normal and dry-run modes.
- Add regression coverage for exclusive terminal ownership, hidden task visibility, progress restart, and exception-safe restoration.
- Validate the complete suite with 1,235 passing tests and 82% overall coverage.
- Confirm `ruff check .` and `black --check .` complete successfully.
- Build `custy-prod:latest` and verify it reports the resolved source version.
- Verify Micro, Nano, Vim, and Vi are present, their bundled settings load, and Custy resolves the intended container priority.

#### Workflow

- Confirm Ruff, Black, and diff-whitespace validation pass.
- Confirm English and Indonesian documentation typechecking and production
- Rebuild `custy-prod:latest` and verify the standalone push profile resolves
- Verify Docker dry-run simulates the configured GitHub and GitLab destinations

### Configurable Editor Resolution

#### Editor

- Add typed `[tool.custy.editor]` settings with separate Windows, Linux, macOS, and container candidate lists.
- Support `vscode`, `notepad`, `micro`, `nano`, `vim`, `vi`, and `neovim`, including the `code` and `nvim` aliases.
- Honor `VISUAL` first and `EDITOR` second when environment precedence is enabled.
- Resolve configured candidates in list order and use built-in platform defaults only when fallback is enabled.
- Validate unsupported identifiers and malformed editor configuration with actionable configuration errors.
- Propagate the resolved editor settings through direct commit/tag commands and supported `custy run` workflows.

### Reliable Local Launching

#### Editor

- Launch the executable path returned by system discovery instead of retrying the unresolved command name.
- Fix Windows installations where `code` resolves to the `code.cmd` shim and previously fell through to Notepad.
- Keep subprocess execution shell-free so file paths and editor arguments are not reinterpreted by a command shell.
- Skip unavailable editors and non-interactive terminal candidates while recording useful diagnostics.
- Stop with `EDITOR_PROCESS_FAILED` when an editor starts but exits unsuccessfully instead of unexpectedly opening another editor.
- Preserve dry-run behavior by reporting the selected editor without launching a process.
- Route workflow message editing through the shared editor service.
- Hide and stop live pipeline progress while a blocking editor owns the terminal, then restore the same step after the editor exits.
- Restore progress state through both successful and failed editor handoffs so Micro, Nano, Vim, and Vi buffers remain unobstructed.

### Docker Editor Experience

#### Editor

- Install Micro, Nano, Vim, and Vi in the production-capable image.
- Prefer Micro for interactive container workflows, followed by Nano, Vim, and Vi.
- Ship Custy-managed Micro, Nano, and Vim configuration under `/etc/custy/editors`.
- Provide `Alt+Z` for undo and `Alt+Y` for redo in Micro and Vim while retaining their native bindings.
- Retain Nano's native `Alt+U` undo and `Alt+E` redo bindings to avoid conflicts with Nano interface shortcuts.
- Mark the image with `CUSTY_CONTAINER=1` so editor selection does not depend only on runtime filesystem heuristics.
- Require `docker run -it` when a terminal editor must receive interactive input.

### Docker Runtime And Packaging

#### Editor

- Include Nano in the production-capable base image for interactive message editing with `docker run -it`.
- Resolve the Custy package version from repository tags and commit distance before building an image.
- Normalize supported SemVer and PEP 440 Git descriptions into valid Python package versions.
- Pass the resolved version into Docker and Compose builds through `CUSTY_BUILD_VERSION`.
- Use setuptools-scm's build-time override so images built without copied `.git` metadata no longer fall back to `0.1.0`.
- Keep a safe `0.1.0` fallback for source archives or environments where repository metadata cannot be resolved.

### Compatibility And Usage Notes

#### Editor

- Interactive container workflows should continue to use `docker run -it` when Custy must open commit or tag messages.
- Non-interactive automation must configure a suitable non-interactive editor or use workflow options that avoid interactive editing.
- Existing projects remain compatible because missing editor configuration uses platform-aware defaults.
- Host editor installations are not automatically available inside a container; container candidates are resolved independently.
- This checkpoint does not create a release tag; its changes will be included in the cumulative v2.1.0 release commit and tag messages.

### Project Layout Detection

- Detect Python, Node.js, PHP, mixed, and generic repositories from conventional root metadata.
- Resolve source directories at command runtime instead of validating a global `app/` path during import.
- Prefer `app/` then `src/` for Python projects, and `src/` then `app/` for Node.js and generic projects.
- Safely fall back to the project root when no conventional source directory exists.
- Keep explicit `project_source` overrides strict and provide actionable configuration errors for missing or invalid paths.
- Treat a missing Custy configuration as an expected uninitialized-project state instead of emitting startup noise.

### Cross Ecosystem Version Metadata

- Add automatic discovery for existing Python `__version__.py`, static `[project].version` in `pyproject.toml`, and string `version` values in `package.json`.
- Synchronize supported Python and Node.js metadata in mixed repositories.
- Update root Node.js lockfile metadata when applicable.
- Normalize tag values before writing project metadata so a leading `v` remains a Git tag prefix rather than part of the stored package version.
- Preserve strict validation for explicitly configured version targets.
- Allow repositories without supported version metadata to use Git-tag-only versioning instead of failing validation.

### Project Aware Initialization

- Generate `project_source = "auto"` and `version_file = "auto"` in newly initialized Custy configuration.
- Preserve existing Python and Node.js version metadata during initialization.
- Create a Python `__version__.py` only for a detected Python project that has no supported version metadata.
- Avoid creating artificial Python files in Node.js, Docusaurus, PHP, documentation-only, or generic repositories.
- Retain recursive copying for nested commit and tag message examples.

### Changelog And Message Template Reliability

- Load the packaged changelog template when an uninitialized project does not yet have `.config/custy/templates/changelog/changelog.j2`.
- Preserve project-owned changelog templates as the highest-priority override after initialization.
- Raise an actionable configuration error when an explicitly configured custom template cannot be found.
- Honor the configured tag-message backup directory instead of falling back to an unrelated default location.
- Add focused coverage for packaged fallback loading, project override precedence, missing custom templates, and tag-message backup routing.

### Developer Workflow And Pre Commit

- Expand Make helpers for installing, updating, validating, cleaning, refreshing, and running pre-commit hooks.
- Invoke pre-commit through its correct Python module name, `python -m pre_commit`, for reliable virtual-environment execution.
- Make `pre-commit-refresh` update revisions, rebuild hook environments, install hooks, and validate all repository files.
- Refresh Ruff from `v0.12.0` to `v0.16.4`, pre-commit-hooks from `v4.6.0` to `v6.0.0`, and Black from `25.1.0` to `26.5.1` for the Python 3.14 development toolchain.
- Use Ruff for linting and automatic lint fixes while keeping Black as the single Python formatter.
- Protect inactive `old`, `example_before`, copied, deprecated, development, and temporary Python references from direct and pre-commit-driven formatting.

### Cli Reliability And Error Handling

- Route console and module execution through a centralized protected entry point.
- Render known configuration and validation failures as concise, actionable Custy errors without duplicate tracebacks.
- Summarize unexpected failures in normal mode and preserve full tracebacks when `--debug` is requested.
- Propagate initialization failures to the shared error boundary instead of printing the same failure twice.
- Make a no-subcommand invocation display root help and exit successfully for interactive and container discovery.
- Limit routine pipeline traceback logging to debug mode.

### Docker And Packaging

- Point the installed `custy` console script at the protected application entry point.
- Verify the production image against a mounted Docusaurus repository with no `app/` directory.
- Confirm that no-command and explicit-help container invocations exit successfully.
- Confirm that `custy --dry-run init --mode all` previews initialization without changing mounted project files.
- Keep TOMLKit available in the production dependency set for project metadata updates.

### Compatibility Notes

- Existing explicit paths such as `project_source = "app"` and `version_file = "app/__version__.py"` remain strict by design.
- Projects that want automatic discovery must change those values to `"auto"` or run initialization in a reviewed replacement workflow.
- Existing editable installations should be reinstalled after updating source so the generated `custy` console script uses the protected entry point.
- The experimental status of `custy workflow` is unchanged.

### Private Package Delivery

#### Distribution

- Publish the `custy` wheel and source distribution to the project-level
- Keep `custy` as both the Python distribution and installed console-command
- Convert reviewed SemVer release tags to canonical PEP 440 package versions,
- Use GitLab's short-lived `CI_JOB_TOKEN` for publication and document deploy
- Keep published versions immutable: duplicate uploads fail instead of

### Validation And Release Safeguards

#### Distribution

- Validate every pushed tag without granting every tag publication authority.
- Let unprotected tags complete package validation successfully while skipping
- Require a protected, non-empty annotated release tag before any external
- Reject unsupported or ambiguous versions, lightweight tags, empty tag
- Build exactly one wheel and one source distribution, run `twine check`,

### Pipeline And Documentation Alignment

#### Distribution

- Order the GitLab release path as test → package validation → production
- Keep retained package artifacts available for inspecting validation-only tag
- Document the protected-tag contract, deploy-token installation, version
- Add regression coverage for validation-only unprotected tags and protected

**Tags**

release • docs • tests • ci • distribution

## v2.0.0 (2026-08-24)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.14...v2.0.0

**Release**
Stable

**Summary**

Promote Custy from `v2.0.0-rc.1` to the stable `v2.0.0` release after the
release-candidate review period.

No new user-facing features or behavioral changes are introduced after RC.1.
This commit finalizes repository housekeeping and preserves the completed 2.0
development history before publishing the stable release.

Promoted from:

- v2.0.0-rc.1

### 🚀 Releases

- Remove temporary root development notes and change-tracking scratch files.
- Remove the completed root and version-specific TODO files.
- Archive the v2.0.0 development scope, decisions, completed work, remaining ideas, and release checklist in `docs/TODO_tracking_history.md`.
- Review the final repository contents so personal and temporary planning files are not included unintentionally.

### Stable Promotion

- Carry forward the complete feature, fix, refactor, test, automation, and documentation set validated during `v2.0.0-rc.1`.
- Keep the public CLI, configuration schema, changelog output, dry-run behavior, pipeline profiles, and extension boundaries unchanged from RC.1.
- Preserve the experimental status and warning for the `custy workflow` branch-policy feature.
- Prepare the repository for the stable SemVer tag and public release publication.

### ✨ Features

#### CLI and workflow orchestration

- Replaced the monolithic `argparse` entrypoint with a Typer-based command layer under `app/cli`.
- Added focused commands for `init`, `validate`, `commit`, `tag`, `push`, `version`, `changelog`, `backup`, and `cleanup`.
- Added `custy run` with explicit `dev`, `release`, and `full` profiles plus safe custom step composition.
- Added global dry-run, debug, and configurable log-level controls with consistent command resolution; dry-run now permits read-only discovery while simulating intended project-file, editor, Git, and remote mutations.
- Added project initialization modes for configuration, templates, examples, and selective scaffolding.

#### Configuration and templates

- Replaced the minimal `.custor.toml` configuration with the namespaced `.config/custy/config.toml` system.
- Added CLI-to-config-to-default resolution for project paths, templates, versioning, execution, push, cleanup, workflow, logging, changelog, and Git behavior.
- Added packaged defaults under `app/templates` while keeping generated user configuration and editable templates under `.config/custy`.
- Added configurable initialization behavior for creating, replacing, or preserving project resources.

#### Changelog generation

- Rebuilt changelog generation as a modular parsing, processing, integration, sorting, rendering, and provider pipeline.
- Added Conventional Commit parsing with type, scope, subsection, and breaking-change support.
- Added configurable type and scope mappings, release behavior, link generation, pending-commit placement, cleaning rules, and Jinja rendering.
- Added commit expansion, deduplication, grouping, release ordering, contributor metadata, statistics, and pending-commit integration.
- Fixed generated `CHANGELOG.md` structure, release summaries, comparison links, grouping, and formatting to produce predictable release output.
- Added determinate Rich progress for `Unreleased` content and every discovered Git tag while collecting changelog history.

#### Git, versioning, and repository maintenance

- Added separated Git command execution and service layers with structured operation results and clearer error handling.
- Preserved SemVer, PEP 440, Commitizen, date, and Git-count strategies behind focused strategy and helper interfaces.
- Added explicit commit staging modes, annotated tag preparation, tag sorting, multi-remote push controls, and backup synchronization.
- Added structured commit/tag-message backup operations and configurable retention cleanup.
- Added safer branch cleanup with protected-branch rules, merge-status filters, age filters, result models, and Rich summaries.
- Added an explicit subprocess dry-run boundary that executes safe Git discovery while returning simulated results for mutating Git operations.

### 🧠 Improvements

#### User experience

- Added Rich panels, tables, changelog progress indicators, consistent colors, clearer summaries, and actionable error messages.
- Expanded command help with examples, related-command guidance, safety notes, and clearer option descriptions.
- Improved validation before mutating operations and made pipeline execution order visible and predictable.
- Added warnings that the workflow-policy command remains experimental.

#### Developer experience

- Added clear boundaries between CLI presentation, core domain logic, Git services, pipelines, configuration, UI, templates, and shared errors.
- Introduced strategy, factory, builder, protocol, service, presenter, and pipeline patterns where they simplify extension and testing.
- Replaced broad utility-module responsibilities with feature-specific packages and smaller focused components.
- Added centralized logging with console and external-file configuration for improved diagnosis and observability.
- Added modular Make targets with discoverable help for local, Docker, Compose, registry, quality, documentation, and release workflows.

### 🧠 Refactoring

- Replaced the former `app/__main__.py` orchestration and large `GitCommitTagger` responsibilities with dedicated CLI and core layers.
- Moved active Git, versioning, workflow, cleanup, backup, changelog, and initialization logic out of the former catch-all `app/utils` layout.
- Split workflow execution into reusable profiles, ordered steps, resolvers, builders, and an execution engine.
- Split the changelog generator into typed models and independently testable parsing, processing, provider, sorting, integration, and rendering components.
- Reorganized project metadata, templates, automation, developer tooling, and repository structure for maintainability.

### 🐛 Bug Fixes

- Corrected changelog grouping, pending-commit handling, release ordering, comparison-link generation, and Markdown rendering behavior.
- Fixed command resolution, missing-command feedback, initialization flow, validation behavior, and CLI help presentation.
- Improved handling for repositories with incomplete history, missing optional resources, invalid configuration, and failed Git operations.
- Corrected backup/template path resolution and cleanup behavior across direct commands and composed pipelines.
- Closed dry-run side-effect gaps across initialization, versioning, changelog, backup, cleanup, commit, tag, push, run, and experimental workflow operations.

### 🧪 Tests

- Expanded coverage from a small version/workflow-focused suite to dedicated CLI, configuration, core, integration, regression, logging, UI, service, and utility tests.
- Added focused coverage for commands, pipeline steps, Git services, version strategies, initialization, cleanup, workflow behavior, and changelog generation.
- Added changelog fixtures and regression coverage for the expected rendered output.
- Added regression coverage for read-only discovery, simulated mutations, preserved files and Git state, backup retention, branch cleanup, and changelog progress reporting.

### 📚 Documentation

- Reworked the README for the current CLI, Python 3.14 requirements, release artifacts, Docker/GHCR usage, Make helpers, and MIT licensing.
- Launched a dedicated Custy documentation hub on the Devalltect Docusaurus portal, replacing the 1.x release line's reliance on repository-local documentation as the primary user guide.
- Published the default English documentation at https://devalltect00.github.io/devalltect-docs/docs/custy and the Indonesian documentation at https://devalltect00.github.io/devalltect-docs/id/docs/custy.
- Documented commands, configuration sections, pipeline profiles, architecture, user workflows, developer workflows, reference material, and troubleshooting guidance.
- Added clearer cross-references to avoid duplicated troubleshooting and configuration content.
- Synchronized English and Indonesian guidance for dry-run resolution, diagnostic logging, changelog progress, and the Git execution boundary.

### ⚙️ CI/CD

- Added GitHub Actions and GitLab CI definitions for validation, container builds, and release workflows.
- Added multi-stage development and production Docker images plus Docker Compose workflows.
- Added pre-commit, Ruff, Black, Pytest, packaging, and repository-quality automation.
- Added wheel and source-distribution generation for tagged GitHub releases.

### ⚠️ Breaking Changes

- Python 3.14 or newer is now required instead of the previous Python 3.11 baseline.
- Configuration moved from `.custor.toml` to `.config/custy/config.toml` with a new namespaced schema.
- User-editable message and changelog templates moved to `.config/custy/templates`; packaged defaults now live under `app/templates`.
- The console entrypoint moved from `app.__main__:main` to the Typer application at `app.cli.main:app`.
- The old `commit-tag-bump` and `all` flows are replaced by focused commands and `custy run` profiles.
- The old `cleaned-backups` and `clean-branches` commands are replaced by `custy cleanup backups` and `custy cleanup branches`.
- Changelog generation now uses `custy changelog generate` and the new configuration-driven rendering pipeline.
- Internal module paths and extension points changed substantially because CLI, core, Git, pipeline, UI, and configuration responsibilities are now separated.

**Tags**

release • feature • bugfix • docs • breaking-change • tests • ci • stable • repository-cleanup • history-archive

## v2.0.0-rc.1 (2026-08-24)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.14...v2.0.0-rc.1

**Release**
Release Candidate

**Summary**

Prepare the first release candidate for Custy 2.0.0, consolidating the major
architecture, CLI, configuration, changelog, automation, testing, and
documentation changes developed after the 1.10.x release line.

This release candidate replaces the former monolithic workflow with explicit,
composable subsystems that are easier to understand, test, debug, maintain, and
extend. It also provides a more consistent experience for users running
individual commands or complete release pipelines.

### ✨ Features

#### CLI and workflow orchestration

- Replaced the monolithic `argparse` entrypoint with a Typer-based command layer under `app/cli`.
- Added focused commands for `init`, `validate`, `commit`, `tag`, `push`, `version`, `changelog`, `backup`, and `cleanup`.
- Added `custy run` with explicit `dev`, `release`, and `full` profiles plus safe custom step composition.
- Added global dry-run, debug, and configurable log-level controls with consistent command resolution; dry-run now permits read-only discovery while simulating intended project-file, editor, Git, and remote mutations.
- Added project initialization modes for configuration, templates, examples, and selective scaffolding.

#### Configuration and templates

- Replaced the minimal `.custor.toml` configuration with the namespaced `.config/custy/config.toml` system.
- Added CLI-to-config-to-default resolution for project paths, templates, versioning, execution, push, cleanup, workflow, logging, changelog, and Git behavior.
- Added packaged defaults under `app/templates` while keeping generated user configuration and editable templates under `.config/custy`.
- Added configurable initialization behavior for creating, replacing, or preserving project resources.

#### Changelog generation

- Rebuilt changelog generation as a modular parsing, processing, integration, sorting, rendering, and provider pipeline.
- Added Conventional Commit parsing with type, scope, subsection, and breaking-change support.
- Added configurable type and scope mappings, release behavior, link generation, pending-commit placement, cleaning rules, and Jinja rendering.
- Added commit expansion, deduplication, grouping, release ordering, contributor metadata, statistics, and pending-commit integration.
- Fixed generated `CHANGELOG.md` structure, release summaries, comparison links, grouping, and formatting to produce predictable release output.
- Added determinate Rich progress for `Unreleased` content and every discovered Git tag while collecting changelog history.

#### Git, versioning, and repository maintenance

- Added separated Git command execution and service layers with structured operation results and clearer error handling.
- Preserved SemVer, PEP 440, Commitizen, date, and Git-count strategies behind focused strategy and helper interfaces.
- Added explicit commit staging modes, annotated tag preparation, tag sorting, multi-remote push controls, and backup synchronization.
- Added structured commit/tag-message backup operations and configurable retention cleanup.
- Added safer branch cleanup with protected-branch rules, merge-status filters, age filters, result models, and Rich summaries.
- Added an explicit subprocess dry-run boundary that executes safe Git discovery while returning simulated results for mutating Git operations.

### 🧠 Improvements

#### User experience

- Added Rich panels, tables, changelog progress indicators, consistent colors, clearer summaries, and actionable error messages.
- Expanded command help with examples, related-command guidance, safety notes, and clearer option descriptions.
- Improved validation before mutating operations and made pipeline execution order visible and predictable.
- Added warnings that the workflow-policy command remains experimental.

#### Developer experience

- Added clear boundaries between CLI presentation, core domain logic, Git services, pipelines, configuration, UI, templates, and shared errors.
- Introduced strategy, factory, builder, protocol, service, presenter, and pipeline patterns where they simplify extension and testing.
- Replaced broad utility-module responsibilities with feature-specific packages and smaller focused components.
- Added centralized logging with console and external-file configuration for improved diagnosis and observability.
- Added modular Make targets with discoverable help for local, Docker, Compose, registry, quality, documentation, and release workflows.

### 🧠 Refactoring

- Replaced the former `app/__main__.py` orchestration and large `GitCommitTagger` responsibilities with dedicated CLI and core layers.
- Moved active Git, versioning, workflow, cleanup, backup, changelog, and initialization logic out of the former catch-all `app/utils` layout.
- Split workflow execution into reusable profiles, ordered steps, resolvers, builders, and an execution engine.
- Split the changelog generator into typed models and independently testable parsing, processing, provider, sorting, integration, and rendering components.
- Reorganized project metadata, templates, automation, developer tooling, and repository structure for maintainability.

### 🐛 Bug Fixes

- Corrected changelog grouping, pending-commit handling, release ordering, comparison-link generation, and Markdown rendering behavior.
- Fixed command resolution, missing-command feedback, initialization flow, validation behavior, and CLI help presentation.
- Improved handling for repositories with incomplete history, missing optional resources, invalid configuration, and failed Git operations.
- Corrected backup/template path resolution and cleanup behavior across direct commands and composed pipelines.
- Closed dry-run side-effect gaps across initialization, versioning, changelog, backup, cleanup, commit, tag, push, run, and experimental workflow operations.

### 🧪 Tests

- Expanded coverage from a small version/workflow-focused suite to dedicated CLI, configuration, core, integration, regression, logging, UI, service, and utility tests.
- Added focused coverage for commands, pipeline steps, Git services, version strategies, initialization, cleanup, workflow behavior, and changelog generation.
- Added changelog fixtures and regression coverage for the expected rendered output.
- Added regression coverage for read-only discovery, simulated mutations, preserved files and Git state, backup retention, branch cleanup, and changelog progress reporting.

### 📚 Documentation

- Reworked the README for the current CLI, Python 3.14 requirements, release artifacts, Docker/GHCR usage, Make helpers, and MIT licensing.
- Launched a dedicated Custy documentation hub on the Devalltect Docusaurus portal, replacing the 1.x release line's reliance on repository-local documentation as the primary user guide.
- Published the default English documentation at https://devalltect00.github.io/devalltect-docs/docs/custy and the Indonesian documentation at https://devalltect00.github.io/devalltect-docs/id/docs/custy.
- Documented commands, configuration sections, pipeline profiles, architecture, user workflows, developer workflows, reference material, and troubleshooting guidance.
- Added clearer cross-references to avoid duplicated troubleshooting and configuration content.
- Synchronized English and Indonesian guidance for dry-run resolution, diagnostic logging, changelog progress, and the Git execution boundary.

### ⚙️ CI/CD

- Added GitHub Actions and GitLab CI definitions for validation, container builds, and release workflows.
- Added multi-stage development and production Docker images plus Docker Compose workflows.
- Added pre-commit, Ruff, Black, Pytest, packaging, and repository-quality automation.
- Added wheel and source-distribution generation for tagged GitHub releases.

### ⚠️ Breaking Changes

- Python 3.14 or newer is now required instead of the previous Python 3.11 baseline.
- Configuration moved from `.custor.toml` to `.config/custy/config.toml` with a new namespaced schema.
- User-editable message and changelog templates moved to `.config/custy/templates`; packaged defaults now live under `app/templates`.
- The console entrypoint moved from `app.__main__:main` to the Typer application at `app.cli.main:app`.
- The old `commit-tag-bump` and `all` flows are replaced by focused commands and `custy run` profiles.
- The old `cleaned-backups` and `clean-branches` commands are replaced by `custy cleanup backups` and `custy cleanup branches`.
- Changelog generation now uses `custy changelog generate` and the new configuration-driven rendering pipeline.
- Internal module paths and extension points changed substantially because CLI, core, Git, pipeline, UI, and configuration responsibilities are now separated.

**Tags**

release • feature • bugfix • docs • breaking-change • tests • ci • rc

## v1.10.14 (2026-04-01)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.13...v1.10.14

**Release**
Stable

**Summary**

Fix branching alignment and repository history consistency after recovery.
Ensures main, develop, and backup branches are synchronized.

**Tags**

release • stable

## v1.10.13 (2026-04-01)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.12...v1.10.13

**Release**
Stable

### 🚀 Releases

- modified `requirements.txt`

**Tags**

release • stable

## v1.10.12 (2026-04-01)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.11...v1.10.12

**Release**
Stable

**Summary**

Final release of **Custy 1.10.12**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

🎉 **Custy 1.10.12 is now stable and ready for production use.**

### 🚀 Releases

- fix bug. Bugs: handling errors when the repository detected is new and no commits yet
- fix little bugs and typo.

**Tags**

release • stable

## v1.10.11 (2025-11-11)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.10...v1.10.11

**Release**
Stable

**Summary**

Final release of **Custy 1.10.11**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

### 🚀 Releases

- modified `requirements.txt`

**Tags**

release • stable

## v1.10.10 (2025-11-11)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.9...v1.10.10

**Release**
Stable

**Summary**

Final release of **Custy 1.10.10**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

### 🚀 Releases

- modified `README.md`
- modified `TODO.md`
- modified `requirements.txt`

**Tags**

release • stable

## v1.10.9 (2025-08-07)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.8...v1.10.9

**Release**
Stable

**Summary**

Final release of **Custy 1.10.9**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

### 🚀 Releases

- fix issue on `.github\workflows\test.yml`

**Tags**

release • stable

## v1.10.8 (2025-08-07)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.7...v1.10.8

**Release**
Stable

**Summary**

Final release of **Custy 1.10.8**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

### 🚀 Releases

- Updated `README.md`

**Tags**

release • stable

## v1.10.7 (2025-08-07)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.6...v1.10.7

**Release**
Stable

**Summary**

Final release of **Custy 1.10.7**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

### 🚀 Releases

- Updated `README.md`

**Tags**

release • stable

## v1.10.6 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.5...v1.10.6

**Release**
Stable

**Summary**

Final release of **Custy 1.10.6**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.
It focuses on refining the WorkflowManager, enhancing commit/tag templates, and introducing safety improvements for merge operations.

Promoted from:

- v1.10.6rc4
- v1.10.6rc3
- v1.10.6rc2
- v1.10.6rc1
- v1.10.6b1
- 1.10.6rc4
- 1.10.6rc3
- 1.10.6rc2
- 1.10.6rc1
- 1.10.6b1

### 🐛 Bug Fixes

- Prevented unsafe execution in CASE 3, 6, and 9 if staged files exist
- Added interactive prompt when no files are staged to confirm whether to continue

#### Workflow

- Ensures safety during critical merge operations inside `WorkflowManager`
- Minor internal logic bugs resolved for better stability

### 🧠 Improvements

- Safer tagging and merging via confirmation-based logic
- More resilient and predictable branching flow

#### Workflow

- Added interactive prompt (`y/n`) when no files are staged during validation
- Allows user to explicitly confirm whether to proceed or abort

### 📄 Templates

- Refactored and expanded `commit-msg.txt` and `tag-msg.txt` example templates

### 📚 Documentation

- Updated project structure docs to reflect current implementation

#### Workflow

- 📘 update `project structure` documentation

**Tags**

release • workflow • bugfix • docs • template • stable

## v1.10.6rc4 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.6rc3...v1.10.6rc4

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.10.6**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release.

### 📄 Templates

#### Template

- Refactored and expanded example templates for commit and tag messages

**Tags**

release • template • rc

## v1.10.6rc3 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.6rc2...v1.10.6rc3

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.10.6**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

### 🐛 Bug Fixes

#### Workflow

- Prevent unintended execution in CASE 3, 6, and 9 if staged files exist
- Ensures safety during critical merge operations inside `WorkflowManager`

### 🧠 Improvements

#### Workflow

- Added interactive prompt (`y/n`) when no files are staged during validation
- Allows user to explicitly confirm whether to proceed or abort

**Tags**

release • workflow • bugfix • rc

## v1.10.6rc2 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.6rc1...v1.10.6rc2

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.10.6**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release:

**Tags**

release • workflow • rc

## v1.10.6rc1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.6b1...v1.10.6rc1

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.10.6**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release.

### 🐛 Bug Fixes

#### Workflow

- Minor internal logic bugs resolved for better stability

### 📚 Documentation

#### Workflow

- 📘 update `project structure` documentation

**Tags**

release • workflow • bugfix • docs • rc

## v1.10.6b1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.5...v1.10.6b1

**Release**
Beta

**Summary**

Beta release for **Custy 1.10.6**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements.

### 🐛 Bug Fixes

#### Workflow

- Minor internal logic bugs resolved for better stability

**Tags**

release • workflow • bugfix • beta

## v1.10.5 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.4...v1.10.5

**Release**
Stable

**Summary**

Final release of **Custy 1.10.5**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

Promoted from:

- v1.10.5rc1
- v1.10.5b1
- 1.10.5rc1
- 1.10.5b1

### 🐛 Bug Fixes

- Minor internal logic bugs resolved for better stability

#### Workflow

- reapplied clean branch functionality

**Tags**

release • bugfix • stable

## v1.10.5rc1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.5b1...v1.10.5rc1

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.10.5**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release.

### 🐛 Bug Fixes

#### Workflow

- Minor internal logic bugs resolved for better stability

**Tags**

release • workflow • bugfix • rc

## v1.10.5b1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.4...v1.10.5b1

**Release**
Beta

**Summary**

Beta release for **Custy 1.10.5**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements:

### 🐛 Bug Fixes

#### Workflow

- reapplied clean branch functionality

**Tags**

release • workflow • bugfix • beta

## v1.10.4 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.3...v1.10.4

**Release**
Stable

**Summary**

Final release of **Custy 1.10.4**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

Promoted from:

- v1.10.4rc1
- v1.10.4b1
- 1.10.4rc1
- 1.10.4b1

### 🐛 Bug Fixes

- Minor internal logic bugs resolved for better stability

### 🧠 Refactoring

- This release just for testing the app for release

**Tags**

release • bugfix • stable

## v1.10.4rc1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.4b1...v1.10.4rc1

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.10.4**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release.

### 🐛 Bug Fixes

- Minor internal logic bugs resolved for better stability

**Tags**

release • bugfix • rc

## v1.10.4b1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.3...v1.10.4b1

**Release**
Beta

**Summary**

Beta release for **Custy 1.10.4**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements:

### 🧠 Refactoring

- This release just for testing the app for release

**Tags**

release • beta

## v1.10.3 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.2...v1.10.3

**Release**
Stable

**Summary**

Final release of **Custy 1.10.3**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

### 🐛 Bug Fixes

- Minor internal logic bugs resolved for better stability

**Tags**

release • bugfix • stable

## v1.10.2 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.1...v1.10.2

**Release**
Stable

**Summary**

Final release of **Custy 1.10.2**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

Promoted from:

- v1.10.2rc1
- v1.10.2b1
- 1.10.2rc1
- 1.10.2b1

### 🐛 Bug Fixes

- Minor internal logic bugs resolved for better stability

#### Workflow

- 🐛 Fixed a minor internal bug to improve stability

**Tags**

release • bugfix • stable

## v1.10.2rc1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.2b1...v1.10.2rc1

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.10.2**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release:

**Tags**

release • workflow • rc

## v1.10.2b1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.1...v1.10.2b1

**Release**
Beta

**Summary**

Beta release for **Custy 1.10.2**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements:

### 🐛 Bug Fixes

#### Workflow

- 🐛 Fixed a minor internal bug to improve stability

**Tags**

release • workflow • bugfix • beta

## v1.10.1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.0...v1.10.1

**Release**
Stable

**Summary**

Final release of **Custy 1.10.1**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

Promoted from:

- v1.10.1rc1
- v1.10.1b2
- v1.10.1b1
- 1.10.1rc1
- 1.10.1b2
- 1.10.1b1

### 🧠 Refactoring

- Code formatted with `ruff`

### 🐛 Bug Fixes

- Minor internal logic bugs resolved for better stability

#### Workflow

- 🐛 Fixed a minor internal bug to improve stability
- Improved commit/tag message template generation logic

**Tags**

release • bugfix • stable

## v1.10.1rc1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.1b2...v1.10.1rc1

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.10.1**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release:

**Tags**

release • rc

## v1.10.1b2 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.1b1...v1.10.1b2

**Release**
Beta

**Summary**

Beta release for **Custy 1.10.1**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements:

### 🐛 Bug Fixes

#### Workflow

- 🐛 Fixed a minor internal bug to improve stability

**Tags**

release • workflow • bugfix • beta

## v1.10.1b1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.0...v1.10.1b1

**Release**
Beta

**Summary**

Beta release for **Custy 1.10.1**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements:

### 🐛 Bug Fixes

#### Workflow

- Improved commit/tag message template generation logic

**Tags**

release • workflow • bugfix • beta

## v1.10.0 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.9.2...v1.10.0

**Release**
Stable

**Summary**

Final release of **Custy 1.10.0**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

Promoted from:

- v1.10.0rc1
- v1.10.0b3
- v1.10.0b2
- v1.10.0b1
- 1.10.0rc1
- 1.10.0b3
- 1.10.0b2
- 1.10.0b1

### ✨ Features

- Full CASE 1–9 support in `WorkflowManager`:
- Core flow: `develop → release → main → post-release`
- Extended handling: `feature/*`, `ci/*`, `archive/*`
- Automatic workflow execution with `run_initial_workflow()` and `run_final_workflow()`
- Backup push handling, context setup, and cleanup automation

#### Workflow

- `run_initial_workflow()` sets up Git context before tag
- `run_final_workflow()` automates merge, cleanup, and post-release steps
- `get_transaction_cases()` includes a complete CASE 1–9 transition map
- Integrated backup push for critical paths

### 📚 Documentation

- Added `git_workflow_cases.md` for CASE logic
- Documented `WorkflowManager` classes and transition behavior
- Updated `README.md`, `TODO.md`, and added usage notes

#### Template

- Reorganized and renamed `commit-msg.txt` and `tag-msg.txt` templates

#### Todo

- Update `docs/TODO.md` to reflect recent changes and clean up outdated entries

#### Workflow

- Documented `WorkflowManager` classes, functions, and transitions
- Added detailed documentation for `WorkflowManager` logic and CASE transitions
- Documented branching behavior, initial/final hooks, and example flow diagrams

### 📄 Templates

- Refactored and expanded example templates for commit and tag messages

#### Workflow

- Added more example templates for `commit-msg.txt` and `tag-msg.txt`

### 🧪 Tests

- Introduced test coverage for `WorkflowManager` and CASE transitions

#### Workflow

- Introduced automated tests for `WorkflowManager` functionality

### 🐛 Bug Fixes

- Fixed transition bugs and branching issues within `WorkflowManager`

#### Workflow

- Fixed logic issues in `WorkflowManager` related to CASE transitions

### ⚠️ Breaking Changes

#### Workflow

- Tag-driven branching now enforced through CASE transition rules

**Tags**

release • workflow • feature • bugfix • docs • template • breaking-change • tests • stable • automation

## v1.10.0rc1 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.0b3...v1.10.0rc1

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.10.0**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release:

### 📚 Documentation

#### Todo

- Update `docs/TODO.md` to reflect recent changes and clean up outdated entries

**Tags**

release • docs • rc • todo

## v1.10.0b3 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.0b2...v1.10.0b3

**Release**
Beta

**Summary**

Beta release for **Custy 1.10.0**, introducing mid-stage docsures and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements:

### 📚 Documentation

#### Template

- Reorganized and renamed `commit-msg.txt` and `tag-msg.txt` templates

**Tags**

release • docs • template • beta

## v1.10.0b2 (2025-08-06)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.10.0b1...v1.10.0b2

**Release**
Beta

**Summary**

Beta release for **Custy 1.10.0**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

Expands test coverage, improves documentation, and refines the logic of the WorkflowManager.

### 📄 Templates

#### Workflow

- Added more example templates for `commit-msg.txt` and `tag-msg.txt`

### 🧪 Tests

#### Workflow

- Introduced automated tests for `WorkflowManager` functionality

### 🐛 Bug Fixes

#### Workflow

- Fixed logic issues in `WorkflowManager` related to CASE transitions

### 📚 Documentation

#### Workflow

- Added `docs/git/git_workflow_cases.md` explaining CASE logic
- Documented `WorkflowManager` classes, functions, and transitions
- Updated `docs/TODO.md` to reflect recent changes

**Tags**

release • workflow • bugfix • docs • template • tests • beta

## v1.10.0b1 (2025-08-05)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.9.2...v1.10.0b1

**Release**
Beta

**Summary**

Beta release for **Custy 1.10.0**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta release introduces full support for CASE-based Git flow automation through an upgraded `WorkflowManager`.

### ✨ Features

#### Workflow

- CASE 1–6: core transitions (develop → release → main → post-release)
- CASE 7–9: extended transitions for `feature/*`, `ci/*`, and `archive/*` branches
- `run_initial_workflow()` sets up Git context before tag
- `run_final_workflow()` automates merge, cleanup, and post-release steps
- `get_transaction_cases()` includes a complete CASE 1–9 transition map
- Integrated backup push for critical paths

### 📚 Documentation

#### Workflow

- Added detailed documentation for `WorkflowManager` logic and CASE transitions
- Documented branching behavior, initial/final hooks, and example flow diagrams

### ⚠️ Breaking Changes

#### Workflow

- Tag-driven branching now enforced through CASE transition rules

**Tags**

release • workflow • feature • docs • breaking-change • beta • automation

## v1.9.2 (2025-08-05)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.9.1...v1.9.2

**Release**
Stable

**Summary**

Final release of **Custy 1.9.2**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

🎉 **Custy 1.9.2 is now stable and ready for production use.**

**Tags**

release • stable • todo.md

## v1.9.1 (2025-08-04)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.9.0...v1.9.1

**Release**
Stable

**Summary**

Final release of **Custy 1.9.1**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

### 🐛 Bug Fixes

- 🐛 Fixed a minor internal bug to improve stability

**Tags**

release • bugfix • stable

## v1.9.0 (2025-08-04)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.8.1...v1.9.0

**Release**
Stable

**Summary**

Final release of **Custy 1.9.0**, promoted from the latest release candidate.

All functionality has been fully validated and production-ready.

Promoted from:

- v1.9.0rc3
- v1.9.0rc2
- v1.9.0rc1
- v1.9.0b3
- v1.9.0b2
- v1.9.0b1
- 1.9.0rc3
- 1.9.0rc2
- 1.9.0rc1
- 1.9.0b3
- 1.9.0b2
- 1.9.0b1

### ✨ Features

- Redesigned CLI interface with support for `--silent` / `--debug` modes
- Smart path resolution for configs and templates to prevent ambiguity
- Improved commit/tag message template generation logic
- Enhanced file path resolution to avoid ambiguity for key config and template files

### 📚 Documentation

- Rewrote and cleaned `README.md`, `HOW_TO_USE.md`, and `docs/TODO.md`
- Added badge list and regenerated project structure documentation
- Updated `docs/TODO.md` to reflect recent changes and clean up outdated tasks
- Confirmed installation steps and usage instructions are ready and verified
- Created user guide: `docs/HOW_TO_USE.md`

### ⚙️ CI/CD

- GitHub Actions and GitLab CI pipelines now support testing and deployment
- Makefile updated with silent execution support (`--no-debug`)
- Added GitHub Actions workflow and GitLab CI pipeline to run tests on push

### 🧠 Refactoring

- Refactored and cleaned up the `app/` structure
- Relocated and organized all templates
- Applied code formatting using `ruff`

### 🐛 Bug Fixes

- Fixed a minor internal bug affecting stability
- Commit type now validated before version resolution
- Fixed pipeline creation issues due to misconfigured jobs
- Resolved internal logic bugs to improve stability
- `assert_is_final_version()` now gracefully handles pre-releases
- Fixed issue where pipeline couldn’t be created due to misconfigured job or script condition

### 📄 Templates

- Added more example templates for `commit-msg.txt` and `tag-msg.txt` to guide custom formatting
- Updated commit/tag message generation to use new structure

### 🧠 Improvements

- Updated Makefile commands to include `--no-debug` for cleaner production runs
- Example: `custy-all-major` now includes `--no-debug` for silent execution
- Tweaked `assert_is_final_version()`:
- From blocking pre-releases to simply skipping generation with a warning
- More graceful handling for versions like `1.9.0b2`, `rc`, `a`, etc.

### 📦 Others

- Updated `Makefile`, `pyproject.toml`, and related config files
- Removed unused file `q`

**Tags**

release • feature • bugfix • docs • template • ci • stable

## v1.9.0rc3 (2025-08-04)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.9.0rc2...v1.9.0rc3

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.9.0**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release:

### 🐛 Bug Fixes

- 🐛 Fixed a minor internal bug to improve stability

**Tags**

release • bugfix • rc

## v1.9.0rc2 (2025-08-04)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.9.0rc1...v1.9.0rc2

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.9.0**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release:

### 🐛 Bug Fixes

- 🐛 Fixed a minor internal bug to improve stability

### 📚 Documentation

- Updated `docs/TODO.md` to reflect recent changes and clean up outdated tasks
- Confirmed installation steps and usage instructions are ready and verified

**Tags**

release • bugfix • docs • rc

## v1.9.0rc1 (2025-08-04)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.9.0b3...v1.9.0rc1

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.9.0**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release:

### 📚 Documentation

- Update `docs/TODO.md` to reflect recent changes and clean up outdated entries

**Tags**

release • docs • rc

## v1.9.0b3 (2025-08-04)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.9.0b2...v1.9.0b3

**Release**
Beta

**Summary**

Beta release for **Custy 1.9.0**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements:

### 🐛 Bug Fixes

- Fixed a minor internal bug affecting stability

**Tags**

release • bugfix • beta

## v1.9.0b2 (2025-08-04)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.9.0b1...v1.9.0b2

**Release**
Beta

**Summary**

Beta release for **Custy 1.9.0**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements:

### 🐛 Bug Fixes

- Fixed a bug where commit type wasn't validated before resolving version, which could lead to invalid version bumps
- Fixed issue where pipeline couldn’t be created due to misconfigured job or script condition

### 📄 Templates

- Added more example templates for `commit-msg.txt` and `tag-msg.txt` to guide custom formatting

### 🧠 Improvements

- Updated Makefile commands to include `--no-debug` for cleaner production runs
- Example: `custy-all-major` now includes `--no-debug` for silent execution
- Tweaked `assert_is_final_version()`:
- From blocking pre-releases to simply skipping generation with a warning
- More graceful handling for versions like `1.9.0b2`, `rc`, `a`, etc.

**Tags**

release • bugfix • template • beta

## v1.9.0b1 (2025-08-04)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.8.1...v1.9.0b1

**Release**
Beta

**Summary**

Beta release for **Custy 1.9.0**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This update delivers improved CLI usability, updated documentation, better automation, refactored project structure, and improved message templates.

### ✨ Features

- Redesigned CLI interface with clearer UX and new arguments (e.g., silent/debug flags)
- Enhanced file path resolution to avoid ambiguity for key config and template files

### 📚 Documentation

- Rewrote `README.md` and added badges (`docs/badges.md`)
- Created user guide: `docs/HOW_TO_USE.md`
- Regenerated project structure documentation

### ⚙️ CI/CD

- Added GitHub Actions workflow and GitLab CI pipeline to run tests on push

### 📄 Templates

- Reorganized and renamed `commit-msg.txt` and `tag-msg.txt` templates
- Updated commit/tag message generation to use new structure

### 🐛 Bug Fixes

- Fixed a minor internal bug affecting stability

### 📦 Others

- Updated `Makefile`, `pyproject.toml`, and related config files
- Removed unused file `q`

**Tags**

release • feature • bugfix • docs • template • ci • beta

## v1.8.1 (2025-08-02)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.8.0...v1.8.1

**Release**
Stable

**Summary**

Final release of **Custy 1.8.1**, promoted from the latest release candidate.

### 🐛 Bug Fixes

#### Push

- auto-push backup/main after changelog generation

**Tags**

release • bugfix • stable • push

## v1.8.0 (2025-08-02)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.7.1...v1.8.0

**Release**
Stable

**Summary**

Final release of **Custy 1.8.0**, promoted from the latest release candidate.

Promoted from:

- v1.8.0rc2
- v1.8.0rc1
- 1.8.0rc2
- 1.8.0rc1

### 🚀 Releases

- Git workflow validation via WorkflowManager
- Temporary branch cleanup with Branch Cleaner
- Dual remote strategy: GitLab (main), GitHub (backup)
- Strategy-specific version helpers (PEP 440, SemVer)
- Cleaner code structure with VersionHelperBase
- Updated Makefile and improved documentation
- Allow `custy all` to run `--sync-backup` automatically
- Minor improvements to support `release` commit type
- Refined docs and cleaned up TODO list

### ⚠️ Breaking Changes

#### Workflow

- classify(): identify version tier
- tier_order(): precedence for tier promotion
- suggest_tag(): next tag suggestion based on branch
- get_transition_cases(): valid CASE transitions
- Update `docs/TODO.md` to reflect recent changes and clean up outdated entries
- Reduced branching logic in check_transition()
- Added docstrings across all helper classes
- Improved extensibility for future strategies (e.g., CalVer)

### 🐛 Bug Fixes

- use 2 permanent branches: `main` (stable) and `develop` (experimental)
- configure dual remote setup: GitLab as main, GitHub as backup
- implement branching workflow checking (WorkflowManager)
- add branch cleaner to prune temporary branches
- update Makefile with latest tasks
- expand documentation
- Allow CLI to accept arguments without explicitily requiring the `--bump` flag
- Fix issue in the `Makefile` execution flow
- Update `docs/TODO.md` to reflect recent changes and clean up outdated entries

**Tags**

release • bugfix • breaking-change • stable

## v1.8.0rc2 (2025-08-01)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.8.0rc1...v1.8.0rc2

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.8.0**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This refactor decouples strategy-specific logic (PEP 440, SemVer) from the core
WorkflowManager class by introducing a VersionHelperBase interface. Both
PEP440VersionHelper and SemverVersionHelper now implement:

### ⚠️ Breaking Changes

#### Workflow

- classify(): identify version tier
- tier_order(): precedence for tier promotion
- suggest_tag(): next tag suggestion based on branch
- get_transition_cases(): valid CASE transitions
- Update `docs/TODO.md` to reflect recent changes and clean up outdated entries
- Reduced branching logic in check_transition()
- Added docstrings across all helper classes
- Improved extensibility for future strategies (e.g., CalVer)

**Tags**

release • workflow • breaking-change • rc

## v1.8.0rc1 (2025-08-01)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.7.1...v1.8.0rc1

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.8.0**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release:

### 🐛 Bug Fixes

- use 2 permanent branches: `main` (stable) and `develop` (experimental)
- configure dual remote setup: GitLab as main, GitHub as backup
- implement branching workflow checking (WorkflowManager)
- add branch cleaner to prune temporary branches
- update Makefile with latest tasks
- expand documentation
- Allow CLI to accept arguments without explicitily requiring the `--bump` flag
- Fix issue in the `Makefile` execution flow
- Update `docs/TODO.md` to reflect recent changes and clean up outdated entries

**Tags**

release • bugfix • rc

## v1.7.1+post.1 (2025-08-01)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.7.0+post.3...v1.7.1+post.1

**Release**
Stable

### 🐛 Bug Fixes

- Allow CLI to accept arguments without explicitily requiring the `--bump` flag
- Fix issue in the `Makefile` execution flow
- Update `docs/TODO.md` to reflect recent changes and clean up outdated entries
- Fix the file name from `pep404_strategy.py` → `pep440_strategy.py`
- Rename class from `PEP404Strategy` → `PEP440Strategy`
- Bug fix: Switch to post-release uses `1.5.0.post1` instead of bumping minor to `1.6.0.post1`
- Refactor and clean up `SemverStrategy` and `PEP440Strategy` classes
- Add test coverage for both strategy classes
- Add documentation: `docs/versioning/switching version.md`
- Verbose step logging before changelog generation to improve debugging
- Ensure commit and tag backup files are staged before commit
- Improve dry-run flow in message generation
- Update `TODO.md` to reflect current feature status

**Tags**

release • bugfix • stable

## v1.7.0+post.3 (2025-07-31)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.6.0+post.2...v1.7.0+post.3

**Release**
Stable

### 📚 Documentation

- modified documentation `docs/TODO.md`

**Tags**

release • docs • stable

## v1.6.0+post.2 (2025-07-31)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.5.0...v1.6.0+post.2

**Release**
Stable

### 🐛 Bug Fixes

- Bug fixed case example changes from 1.5.0.post1 → 1.5.0.post2
- Bug fixed on `Makefile`
- modified documentation `docs/TODO.md`

### 📚 Documentation

- Added a new documentation `docs/Q&A/git/footer section on tag message.md`
- modified documentations `docs/Q&A/git/changelogs approach.md`, `docs/Q&A/git/comparison commit and tag message.md`, `docs/TODO.md`
- Modified Makefile by adding documentations

**Tags**

release • bugfix • docs • stable

## v1.5.0+post.1 (2025-07-31)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.4.3...v1.5.0+post.1

**Release**
Stable

### 📚 Documentation

- Added a new documentation `docs/Q&A/git/footer section on tag message.md`
- modified documentations `docs/Q&A/git/changelogs approach.md`, `docs/Q&A/git/comparison commit and tag message.md`, `docs/TODO.md`
- Modified Makefile by adding documentations

### ✨ Features

- Add structured generation of `commit-msg.txt` and `tag-msg.txt`
- Improve message clarity, flexibility, and consistency
- Enhance template customization for different version types (alpha, beta, rc, final)
- Ensure commit/tag message files are properly staged and backed up
- Fix dry-run behavior in message generation flow
- Address minor edge cases in automation
- Reference commit/tag standards in `docs\git\commit_message.md`
- Add internal usage notes and automation guidance
- Handle staged and commit when no staged changes detected
- Updated `TODO.md`

**Tags**

release • feature • docs • stable

## v1.5.0rc1 (2025-07-31)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.5.0b3...v1.5.0rc1

**Release**
Release Candidate

**Summary**

Release candidate for **Custy 1.5.0**, consolidating all pre-release changes and preparing for stable release.
Feature-complete and undergoing final validation before stable release.

This release candidate consolidates finalized features and bug fixes before the stable release:

**Tags**

release • rc

## v1.5.0b3 (2025-07-31)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.5.0b2...v1.5.0b3

**Release**
Beta

**Summary**

Beta release for **Custy 1.5.0**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements:

**Tags**

release • beta

## v1.5.0b2 (2025-07-30)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.5.0b1...v1.5.0b2

**Release**
Beta

**Summary**

Beta release for **Custy 1.5.0**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements:

**Tags**

release • beta

## v1.5.0b1 (2025-07-30)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.4.3...v1.5.0b1

**Release**
Beta

**Summary**

Beta release for **Custy 1.5.0**, introducing mid-stage features and workflow improvements.
Partially validated features and improvements. Some issue still remain.

This beta includes several bug fixes and enhancements:

**Tags**

release • beta

## v1.4.3 (2025-07-29)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.4.2...v1.4.3

**Release**
Stable

**Summary**

Final release of Custy v1.4.3

Refactor Code using Ruff

**Tags**

release • stable

## v1.4.2 (2025-07-29)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.4.1...v1.4.2

**Release**
Stable

**Tags**

release • stable

## v1.4.1 (2025-07-29)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.4.0...v1.4.1

**Release**
Stable

**Summary**

Final release of Custy v1.4.1

Includes bug fixes.

**Tags**

release • stable

## v1.4.0 (2025-07-29)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.3.3...v1.4.0

**Release**
Stable

**Summary**

Final release of Custy v1.4.0 - prompted from release candidate.

Promoted from:

- v1.4.0rc3
- v1.4.0rc2
- v1.4.0rc1
- v1.4.0a1

### 🐛 Bug Fixes

- Resolved issue with `get_commits_between_tags()` in `GitHelper`(`app\utils\git.py`).
- Resolved issue with `_prepare_release_message_from_prereleases()` in `GitCommitTagger`(`app\git_commit_tagger.py`).

### ✨ Features

- Prevents major.minor.patch bump when incrementing within same pre-release tier (e.g. rc1 → rc2)
- Generates changelog only on final releases (e.g. v1.2.3), skipping pre-releases
- Enforces bump/tag only for specific types [feat, fix, perf, docs, refactor]; others require CLI override
- Detects project type (Python or JavaScript) to apply correct versioning scheme (PEP 440 or SemVer)
- Tag messages now opened in external file like commit-msg.txt; supports default message via flag
- For release tags, auto-generates commit/tag message template from all prereleases (pre, dev, etc.)
- Prevents bump from rc → final from incrementing patch (v1.2.3rc1 → v1.2.3, not v1.2.4)
- Adds cleanup logic for `backups/`, keeping 10 latest (customizable via CLI)
- Fixes backup and template folder resolution; commit-msg.txt placement corrected
- Updated `.gitignore` to ignore `templates/tag-msg.txt`

**Tags**

release • feature • bugfix • stable

## v1.4.0rc3 (2025-07-29)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.4.0rc2...v1.4.0rc3

**Release**
Release Candidate

### 🐛 Bug Fixes

- Resolved issue with `get_commits_between_tags()` in `GitHelper`(`app\utils\git.py`).

**Tags**

release • bugfix • rc

## v1.4.0rc2 (2025-07-29)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.4.0rc1...v1.4.0rc2

**Release**
Release Candidate

### 🐛 Bug Fixes

- Resolved issue with `_prepare_release_message_from_prereleases()` in `GitCommitTagger`(`app\git_commit_tagger.py`).

**Tags**

release • bugfix • rc

## v1.4.0rc1 (2025-07-29)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.4.0a1...v1.4.0rc1

**Release**
Release Candidate

**Summary**

Release candidate for v1.4.0, consolidating all pre-release changes.

Includes:

### 🐛 Bug Fixes

- Resolved issue with `get_last_tag_before()` in `GitHelper`(`app\utils\git.py`).

**Tags**

release • bugfix • rc

## v1.4.0a1 (2025-07-29)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.3.3...v1.4.0a1

**Release**
Alpha

### ✨ Features

- Prevents major.minor.patch bump when incrementing within same pre-release tier (e.g. rc1 → rc2)
- Generates changelog only on final releases (e.g. v1.2.3), skipping pre-releases
- Enforces bump/tag only for specific types [feat, fix, perf, docs, refactor]; others require CLI override
- Detects project type (Python or JavaScript) to apply correct versioning scheme (PEP 440 or SemVer)
- Tag messages now opened in external file like commit-msg.txt; supports default message via flag
- For release tags, auto-generates commit/tag message template from all prereleases (pre, dev, etc.)
- Prevents bump from rc → final from incrementing patch (v1.2.3rc1 → v1.2.3, not v1.2.4)
- Adds cleanup logic for `backups/`, keeping 10 latest (customizable via CLI)
- Fixes backup and template folder resolution; commit-msg.txt placement corrected
- Updated `.gitignore` to ignore `templates/tag-msg.txt`

**Tags**

release • feature • alpha

## v1.3.3 (2025-07-25)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.3.2...v1.3.3

**Release**
Stable

### 📚 Documentation

#### Todo.Md

- add or change todo

**Tags**

release • docs • stable

## v1.3.2 (2025-07-25)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.2.0...v1.3.2

**Release**
Stable

### 📚 Documentation

#### Todo.Md

- add or change todo

### ✨ Features

#### Versioning

- Fix pre-release version format to comply with pep440 for python
- Add support for full format: [Epoch!]MAJOR.MINOR.PATCH[Pre-release][Post-release][Development][+Local]

**Tags**

release • feature • docs • stable

## v1.3.1rc2 (2025-07-25)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.3.0rc1...v1.3.1rc2

**Release**
Release Candidate

**Tags**

release • rc

## v1.3.0rc1 (2025-07-25)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.2.0...v1.3.0rc1

**Release**
Release Candidate

### ✨ Features

#### Versioning

- Fix pre-release version format to comply with pep440 for python
- Add support for full format: [Epoch!]MAJOR.MINOR.PATCH[Pre-release][Post-release][Development][+Local]

**Tags**

release • feature • rc

## v1.2.0 (2025-07-25)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.1.5...v1.2.0

**Release**
Stable

### ✨ Features

#### Git Commit Tagger

- Replaces Commitizen's `cz check` with a built-in custom validator
- Supports multi-line messages with header, body, and footer.
- Includes structured error handling via custom ValidationError exception.

**Tags**

release • feature • stable

## v1.1.5 (2025-07-25)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.1.4...v1.1.5

**Release**
Stable

### 🐛 Bug Fixes

#### Git Commit Tagger

- Fix issue where added to stage and commit after change version on `.cz.toml` and `app/__version__.py` files

**Tags**

release • bugfix • stable

## v1.1.4 (2025-07-25)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.1.3...v1.1.4

**Release**
Stable

### 📚 Documentation

#### Todo.Md

- add or change todo

**Tags**

release • docs • stable

## v1.1.3 (2025-07-25)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v1.1.2...v1.1.3

**Release**
Stable

### 🐛 Bug Fixes

#### Makefile

- Fixed broken or unclear `Makefile` commands

**Tags**

release • bugfix • stable

## v1.1.2 (2025-07-25)

🔗 Compare:
https://github.com/devalltect00/Custy/compare/v0.0.1...v1.1.2

**Release**
Stable

**Tags**

release • stable

## v0.0.1 (2025-07-25)

**Release**
Stable

### 🐛 Bug Fixes

#### Makefile

- Fixed broken or unclear `Makefile` commands
- Updated `README.md` for clarity
- Added CLI command docs to `docs/cli_commands_custy.md`
- Modified `docs/project_structure.md`
- Removed unnecessary `app/__init__.py` to silence warning
- Updated `.gitignore` to ignore `commit-msg.txt` and allow `TODO.md`
- Added more documentation under `docs/versioningd/`

### 📦 Others

- Initial commit

**Tags**

release • bugfix • stable
